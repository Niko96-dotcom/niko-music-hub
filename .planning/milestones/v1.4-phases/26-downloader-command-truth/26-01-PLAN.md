---
phase: 26-downloader-command-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/FeatureDownloader/YtDlpDownloader.swift
  - Sources/FeatureDownloader/DownloaderUseCase.swift
  - Tests/FeatureDownloaderTests/DownloadProgressParsingTests.swift
  - Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift
  - Tests/FeatureDownloaderTests/DownloaderUseCaseTests.swift
autonomous: true
requirements:
  - CMD-01
  - CMD-02
must_haves:
  truths:
    - "yt-dlp download invocations emit NIKO_PROGRESS: lines from --progress --progress-template"
    - "DownloaderUseCase updates job progress from NIKO_PROGRESS: markers on every parsed line"
    - "Legacy [download] X% lines still advance progress during transition"
    - "Actual download ExternalProcessRequest has no fixed total timeout (timeoutSeconds nil)"
  artifacts:
    - path: Sources/FeatureDownloader/YtDlpDownloader.swift
      provides: "Real yt-dlp args with --progress and NIKO_PROGRESS template; nil download timeout"
      contains: "NIKO_PROGRESS:"
    - path: Sources/FeatureDownloader/DownloaderUseCase.swift
      provides: "Unified progress parsing for NIKO_PROGRESS and legacy [download] lines"
    - path: Tests/FeatureDownloaderTests/DownloadProgressParsingTests.swift
      provides: "Parser fixtures for NIKO_PROGRESS template output shape"
  key_links:
    - from: YtDlpDownloader.download
      to: ExternalProcessRequest.timeoutSeconds
      via: "omit timeoutSeconds (nil) for real downloads"
      pattern: "timeoutSeconds: nil"
    - from: DownloaderUseCase.downloadWithRetry
      to: DownloaderUseCase.parseProgress
      via: "progressHandler calls parseProgress on every streamed line"
      pattern: "parseProgress\\(from:"
---

<objective>
Align the real yt-dlp command with parseable progress markers and remove the fixed 90-second total download kill.

Purpose: Fix the highest-impact bug — progress template emits `download:%progress` while parsers expect `[download] X%`, so the UI stays at 0% during real downloads (CMD-01). Long valid downloads must not be terminated solely by a stopwatch (CMD-02).
Output: NIKO_PROGRESS template on the command, shared parsers in downloader + use case, nil download timeout, unit tests proving args and parsing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-downloader-command-truth/26-CONTEXT.md
@.planning/ROADMAP.md
@Sources/FeatureDownloader/YtDlpDownloader.swift
@Sources/FeatureDownloader/DownloaderUseCase.swift
@Sources/AppCore/Services/ExternalProcessRunning.swift

<interfaces>
From ExternalProcessRunning.swift:
```swift
public struct ExternalProcessRequest {
    public var timeoutSeconds: TimeInterval?  // nil = no total timeout
}
```

Current mismatch (YtDlpDownloader line 105 vs DownloaderUseCase.parseProgress):
- Command: `--progress-template`, `download:%progress`
- Parser: requires `line.contains("[download]")`

Locked decision: emit `NIKO_PROGRESS:%(progress)s` with `--progress`; keep legacy `[download]` fallback in parsers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NIKO_PROGRESS parser + command args tests</name>
  <files>Tests/FeatureDownloaderTests/DownloadProgressParsingTests.swift, Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift</files>
  <behavior>
    - Test: `NIKO_PROGRESS: 45.2%` parses to 45.2 (YtDlpDownloader) and 0.452 (DownloaderUseCase normalized)
    - Test: legacy `[download] 45.2% of ...` still parses
    - Test: download request args include `--progress`, `--progress-template`, `NIKO_PROGRESS:%(progress)s`
    - Test: download request `timeoutSeconds` is nil (not 90)
    - Test: non-progress lines (info, file markers) return nil
  </behavior>
  <action>
    Add failing tests first per locked progress-marker decision.

    In `DownloadProgressParsingTests`, add cases for `NIKO_PROGRESS:` lines matching the real template shape (e.g. `NIKO_PROGRESS: 10.0%`, `NIKO_PROGRESS:100%`). Keep existing `[download]` cases.

    In `YtDlpDownloaderTests`, update `testDownloadAppliesBoundedNetworkRetries` to assert `timeoutSeconds == nil` instead of 90. Add `testDownloadEmitsNIKOProgressTemplate` asserting arguments contain `--progress`, `--progress-template`, and `NIKO_PROGRESS:%(progress)s`.

    Do not remove legacy parser tests — transition requires both paths.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter DownloadProgressParsingTests 2>&1 | tail -5</automated>
  </verify>
  <done>Red tests exist for NIKO_PROGRESS parsing and nil timeout before production changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Wire NIKO_PROGRESS template and remove download timeout</name>
  <files>Sources/FeatureDownloader/YtDlpDownloader.swift</files>
  <action>
    In `YtDlpDownloader.download` (per locked progress-marker decision):

    1. Add `--progress` to args.
    2. Replace `--progress-template`, `download:%progress` with `--progress-template`, `NIKO_PROGRESS:%(progress)s`.
    3. Set `ExternalProcessRequest.timeoutSeconds` to `nil` (remove `90`). `FoundationExternalProcessRunner` already treats nil as unlimited total duration.

    Refactor `YtDlpDownloader.parseProgressPercentage(from:)` to parse `NIKO_PROGRESS:` prefix first (extract percentage after colon), then fall back to legacy `[download]` regex. Keep method `internal`/`static` as today.

    Do not add stall logic or simulate changes in this plan.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter YtDlpDownloaderTests 2>&1 | tail -8</automated>
  </verify>
  <done>Real download command emits NIKO_PROGRESS markers and has no 90s total timeout.</done>
</task>

<task type="auto">
  <name>Task 3: Align DownloaderUseCase progress parsing</name>
  <files>Sources/FeatureDownloader/DownloaderUseCase.swift, Tests/FeatureDownloaderTests/DownloaderUseCaseTests.swift</files>
  <action>
    Update `DownloaderUseCase.parseProgress(from:)` to mirror `YtDlpDownloader.parseProgressPercentage`:

    1. If line has prefix `NIKO_PROGRESS:`, extract percentage and return value / 100.0 in 0...1 range.
    2. Else fall back to existing `[download]` percentage regex.

    Add `DownloaderUseCaseTests.testParseProgressFromNIKOProgressMarker` using a package-visible test hook (e.g. `@testable` + `internal static` exposure of `parseProgress` via a small `DownloaderProgressParsing` enum if needed to avoid widening API).

    Update `SuccessfulDownloader` / streaming fakes in tests to emit `NIKO_PROGRESS:` lines where they simulate progress, while keeping one legacy-line test.

    Ensure `downloadWithRetry` calls `progress.update` on every parsed marker with no extra throttle (per locked decision).
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter 'DownloadProgressParsingTests|YtDlpDownloaderTests|DownloaderUseCaseTests' 2>&1 | tail -10</automated>
  </verify>
  <done>CMD-01 and CMD-02 satisfied: UI progress path parses real template output; downloads are not killed at 90s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yt-dlp stdout/stderr → Swift parser | Untrusted external process output parsed for UI progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | parseProgress | accept | Progress values clamped 0...1; no code execution from log lines |
| T-26-02 | D | nil timeout download | mitigate | Stall detection added in plan 26-02 (CMD-03); unlimited duration only with activity monitoring |
</threat_model>

<verification>
- `swift test --filter DownloadProgressParsingTests` green
- `swift test --filter YtDlpDownloaderTests` green (timeout nil, NIKO_PROGRESS args)
- `swift test --filter DownloaderUseCaseTests` green
</verification>

<success_criteria>
- CMD-01: Captured download args include `--progress` + `NIKO_PROGRESS:` template; parsers accept template output; job progress updates from markers.
- CMD-02: No `timeoutSeconds: 90` on download requests; tests prove nil timeout.
</success_criteria>

<output>
After completion, create `.planning/phases/26-downloader-command-truth/26-01-SUMMARY.md`
</output>
