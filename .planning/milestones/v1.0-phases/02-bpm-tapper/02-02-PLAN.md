---
phase: 02-bpm-tapper
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Package.swift
  - Sources/OutsideCubaseHub/AppComposition.swift
  - Sources/FeatureBPMTapper/BPMTapperFeature.swift
  - Sources/FeatureBPMTapper/BPMTapperView.swift
  - Sources/FeatureBPMTapper/BPMTapperViewModel.swift
  - Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift
autonomous: true
requirements: ["BPM-01", "BPM-02", "BPM-03", "BPM-05"]
user_setup: []
must_haves:
  truths:
    - "[BPM-01] [D-01] Mouse clicks count only through the large focused BPM tap surface."
    - "[BPM-01] [D-02] Spacebar is the only keyboard input that records a tap, and only while the BPM tool surface is focused."
    - "[BPM-02] [D-07] The UI shows no fake BPM after one tap and shows the first estimate after the second tap."
    - "[BPM-03] [D-04] Reset is available through a visible `Reset Taps` button and the Escape key."
    - "[BPM-05] BPM Tapper is registered through `ToolFeature` and `AppComposition`, not hard-coded in `AppShellView`."
    - "[UI-SPEC] The BPM detail view uses a compact top-leading native SwiftUI surface with a large stable tap pad, terse status text, and Output Inbox secondary."
  artifacts:
    - path: Sources/FeatureBPMTapper/BPMTapperFeature.swift
      provides: "ToolFeature metadata and view factory for BPM Tapper"
      min_lines: 35
      contains: "BPMTapperFeature"
    - path: Sources/FeatureBPMTapper/BPMTapperView.swift
      provides: "Focused SwiftUI BPM tap surface, live readout, status text, and reset button"
      min_lines: 120
      contains: "Tap Tempo"
    - path: Sources/FeatureBPMTapper/BPMTapperViewModel.swift
      provides: "MainActor view model connecting tap events to TempoEstimator"
      min_lines: 80
      contains: "recordTap"
    - path: Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift
      provides: "View model tests for tap, first estimate, reset, and long pause behavior"
      min_lines: 70
      contains: "testSecondTapShowsFirstEstimate"
  key_links:
    - from: Sources/OutsideCubaseHub/AppComposition.swift
      to: Sources/FeatureBPMTapper/BPMTapperFeature.swift
      via: "composition root imports FeatureBPMTapper and registers BPMTapperFeature()"
      pattern: "BPMTapperFeature\\(\\)"
    - from: Sources/FeatureBPMTapper/BPMTapperViewModel.swift
      to: Sources/FeatureBPMTapper/TempoEstimator.swift
      via: "view model delegates tap math to TempoEstimator"
      pattern: "TempoEstimator"
    - from: Sources/FeatureBPMTapper/BPMTapperView.swift
      to: Sources/FeatureBPMTapper/BPMTapperViewModel.swift
      via: "view calls recordTap() and resetTaps() on the view model"
      pattern: "recordTap"
---

<objective>
Register BPM Tapper as the first real tool and build the focused native tap UI.

Purpose: Phase 2 must prove that useful tools can be added through the Phase 1 feature boundary while feeling faster than a browser BPM tapper. This plan wires the BPM feature target into the app, renders a compact SwiftUI tap surface, and supports mouse tapping, focused Space tapping, Escape reset, and a live BPM readout.

Output: `BPMTapperFeature`, `BPMTapperView`, `BPMTapperViewModel`, app registration in `AppComposition`, executable target dependency, and view model tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bpm-tapper/02-CONTEXT.md
@.planning/phases/02-bpm-tapper/02-UI-SPEC.md
@.planning/phases/02-bpm-tapper/02-RESEARCH.md
@.planning/phases/02-bpm-tapper/02-PATTERNS.md
@.planning/phases/02-bpm-tapper/02-01-SUMMARY.md
@Package.swift
@Sources/OutsideCubaseHub/AppComposition.swift
@Sources/OutsideCubaseHub/DevTool/DevToolFeature.swift
@Sources/OutsideCubaseHub/AppShell/AppShellView.swift
</context>

<threat_model>
Trust boundaries:
- Keyboard events are local UI input and must remain scoped to the active BPM tool.
- The app shell trusts registered `ToolFeature` metadata, not hard-coded future tool cases.
- The BPM UI reads local tap state only and should not use output inbox, jobs, network, or helper tools.

STRIDE register:
- threat_id: T-02-FOCUS
  category: Spoofing
  component: BPMTapperView
  disposition: mitigate
  severity: medium
  mitigation_plan: Use a focusable tap surface and handle Space/Escape only while that surface is focused.
- threat_id: T-02-REGISTRY
  category: Tampering
  component: AppComposition
  disposition: mitigate
  severity: medium
  mitigation_plan: Register `BPMTapperFeature()` through the static feature array and avoid `AppShellView` switch/case changes.
- threat_id: T-02-UISTATE
  category: Denial of Service
  component: BPMTapperViewModel
  disposition: mitigate
  severity: low
  mitigation_plan: Keep reset deterministic and preserve stable layout states for idle, one tap, first estimate, and stable estimate.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Register BPMTapperFeature through Package and AppComposition</name>
  <files>Package.swift, Sources/OutsideCubaseHub/AppComposition.swift, Sources/FeatureBPMTapper/BPMTapperFeature.swift</files>
  <read_first>Package.swift, Sources/OutsideCubaseHub/AppComposition.swift, Sources/OutsideCubaseHub/DevTool/DevToolFeature.swift, Sources/AppCore/Features/ToolFeature.swift, .planning/phases/02-bpm-tapper/02-UI-SPEC.md, .planning/phases/02-bpm-tapper/02-PATTERNS.md</read_first>
  <action>Update the `OutsideCubaseHub` executable target dependencies in `Package.swift` from `["AppCore"]` to `["AppCore", "FeatureBPMTapper"]`. Create `Sources/FeatureBPMTapper/BPMTapperFeature.swift` with `public struct BPMTapperFeature: ToolFeature`, metadata id exactly `"bpm-tapper"`, displayName exactly `"BPM Tapper"`, shortLabel exactly `"BPM Tapper"`, systemImage exactly `"metronome"`, and capabilities exactly `[]`. Implement `@MainActor public func makeView(context: ToolContext) -> AnyView` returning `AnyView(BPMTapperView(context: context))`. Update `Sources/OutsideCubaseHub/AppComposition.swift` to `import FeatureBPMTapper` and register features exactly `[DevToolFeature(), BPMTapperFeature()]`. Do not modify `AppShellView` or `ToolSidebarView` for BPM-specific branching.</action>
  <verify>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter FeatureRegistryTests && ! rg -n "bpm-tapper|BPMTapper" Sources/OutsideCubaseHub/AppShell</verify>
  <acceptance_criteria>
    - `Package.swift` contains `"AppCore", "FeatureBPMTapper"`
    - `Sources/FeatureBPMTapper/BPMTapperFeature.swift` contains `public struct BPMTapperFeature`
    - `Sources/FeatureBPMTapper/BPMTapperFeature.swift` contains `id: "bpm-tapper"`
    - `Sources/FeatureBPMTapper/BPMTapperFeature.swift` contains `displayName: "BPM Tapper"`
    - `Sources/FeatureBPMTapper/BPMTapperFeature.swift` contains `systemImage: "metronome"`
    - `Sources/FeatureBPMTapper/BPMTapperFeature.swift` contains `makeView(context: ToolContext) -> AnyView`
    - `Sources/OutsideCubaseHub/AppComposition.swift` contains `import FeatureBPMTapper`
    - `Sources/OutsideCubaseHub/AppComposition.swift` contains `BPMTapperFeature()`
    - `rg -n "bpm-tapper|BPMTapper" Sources/OutsideCubaseHub/AppShell` returns no matches
    - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter FeatureRegistryTests` exits 0
  </acceptance_criteria>
  <done>BPM Tapper is available through the registry without shell-specific code.</done>
</task>

<task type="auto">
  <name>Task 2: Build BPMTapperViewModel and focused tap surface</name>
  <files>Sources/FeatureBPMTapper/BPMTapperViewModel.swift, Sources/FeatureBPMTapper/BPMTapperView.swift</files>
  <read_first>Sources/FeatureBPMTapper/TempoEstimator.swift, Sources/FeatureBPMTapper/BPMAdjustment.swift, Sources/OutsideCubaseHub/DevTool/DevToolFeature.swift, Sources/OutsideCubaseHub/AppShell/AppShellView.swift, .planning/phases/02-bpm-tapper/02-UI-SPEC.md</read_first>
  <action>Create `@MainActor public final class BPMTapperViewModel: ObservableObject` with published properties for `rawBPM: Double?`, `displayedBPM: Double?`, `tapCount: Int`, `statusText: String`, `statusKind`, and `hasStartedRun: Bool`. Add methods `recordTap(at timestamp: TimeInterval = Date().timeIntervalSinceReferenceDate)` and `resetTaps()`. `recordTap` must call `TempoEstimator.tap(at:)`, set idle/one-tap/two-tap/stable/outlier/long-pause status copy exactly from the UI-SPEC table where applicable (`Tap the pad or press Space`, `First estimate ready. Keep tapping to steady it.`, `Stable average from recent taps.`, `New tap run started.`, `Ignored one uneven tap. Keep tapping.`). Create `BPMTapperView` with a compact top-leading layout, title `BPM Tapper`, one short status line, a BPM readout using 40px semibold type, a stable tap surface with minimum size 320 by 180 and ideal size about 440 by 220, visible `Tap Tempo` label, and a visible `Reset Taps` button. Use `@FocusState` and `.focusable()` so the tap surface can receive focus; handle `.onKeyPress(.space)` by calling `recordTap()` and returning `.handled`; handle `.onKeyPress(.escape)` by calling `resetTaps()` and returning `.handled`. Disable copy/save/history UI in this plan; Plan 02-03 adds those actions.</action>
  <verify>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMTapperViewModelTests</verify>
  <acceptance_criteria>
    - `Sources/FeatureBPMTapper/BPMTapperViewModel.swift` contains `final class BPMTapperViewModel`
    - `Sources/FeatureBPMTapper/BPMTapperViewModel.swift` contains `@Published`
    - `Sources/FeatureBPMTapper/BPMTapperViewModel.swift` contains `func recordTap`
    - `Sources/FeatureBPMTapper/BPMTapperViewModel.swift` contains `func resetTaps`
    - `Sources/FeatureBPMTapper/BPMTapperViewModel.swift` contains `First estimate ready. Keep tapping to steady it.`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `Tap Tempo`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `Reset Taps`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `@FocusState`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `.onKeyPress(.space)`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `.onKeyPress(.escape)`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `minWidth: 320`
    - `Sources/FeatureBPMTapper/BPMTapperView.swift` contains `minHeight: 180`
  </acceptance_criteria>
  <done>The BPM tool renders a focused tap surface with live tempo state and reset behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Cover view model tap, reset, and pause behavior</name>
  <files>Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift</files>
  <read_first>Sources/FeatureBPMTapper/BPMTapperViewModel.swift, Sources/FeatureBPMTapper/TempoEstimator.swift, .planning/phases/02-bpm-tapper/02-VALIDATION.md</read_first>
  <action>Add `BPMTapperViewModelTests` using XCTest. Include tests named exactly `testInitialStatePromptsForTap`, `testSecondTapShowsFirstEstimate`, `testResetClearsCurrentRun`, `testLongPauseShowsFreshRunStatus`, and `testOutlierKeepsPreviousDisplayedBPM`. Use deterministic timestamps by calling `recordTap(at:)` with explicit `TimeInterval` values such as `0.0`, `0.5`, `1.0`, `1.5`, and a long pause at `5.0`. Assert that one tap leaves `displayedBPM == nil`, two taps produce approximately `120`, reset clears `rawBPM` and `displayedBPM`, and an outlier keeps the previous displayed BPM. Do not instantiate SwiftUI views in these tests.</action>
  <verify>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMTapperViewModelTests</verify>
  <acceptance_criteria>
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `testInitialStatePromptsForTap`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `testSecondTapShowsFirstEstimate`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `testResetClearsCurrentRun`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `testLongPauseShowsFreshRunStatus`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `testOutlierKeepsPreviousDisplayedBPM`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `XCTAssertNil`
    - `Tests/FeatureBPMTapperTests/BPMTapperViewModelTests.swift` contains `accuracy:`
    - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMTapperViewModelTests` exits 0
    - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test` exits 0
  </acceptance_criteria>
  <done>Focused BPM UI state has regression coverage before action/history polish lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMTapperViewModelTests` exits 0.
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter FeatureRegistryTests` exits 0.
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test` exits 0.
- [ ] `rg -n "bpm-tapper|BPMTapper" Sources/OutsideCubaseHub/AppShell` returns no matches.
- [ ] `Sources/OutsideCubaseHub/AppComposition.swift` registers `BPMTapperFeature()`.
</verification>

<success_criteria>
- All tasks completed.
- BPM-01 mouse/keyboard tap behavior is represented in focused UI and view model state.
- BPM-02 live estimate behavior is visible after two taps and tested.
- BPM-05 is preserved by local feature registration and no network dependency.
- D-01, D-02, D-04, and D-07 are visible in source, tests, or summary.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bpm-tapper/02-02-SUMMARY.md`.
</output>
