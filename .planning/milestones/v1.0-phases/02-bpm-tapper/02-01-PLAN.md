---
phase: 02-bpm-tapper
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Sources/FeatureBPMTapper/TempoEstimator.swift
  - Sources/FeatureBPMTapper/BPMAdjustment.swift
  - Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift
  - Tests/FeatureBPMTapperTests/BPMAdjustmentTests.swift
autonomous: true
requirements: ["BPM-02", "BPM-03", "BPM-05"]
user_setup: []
must_haves:
  truths:
    - "[BPM-02] [D-03] A long pause starts a fresh tap run instead of carrying stale intervals forward."
    - "[BPM-02] [D-05] BPM is calculated from recent intervals with explicit guardrails for unusual taps."
    - "[BPM-02] [D-06] The estimator uses the last 4 accepted intervals for the displayed raw BPM."
    - "[BPM-02] [D-07] The first BPM estimate is available after exactly 2 taps."
    - "[BPM-02] [D-08] Obvious outlier intervals are ignored while the current tap run remains alive."
    - "[BPM-03] [D-09] Original, half-time, and double-time adjustment math preserves the raw tapped BPM as context."
    - "[BPM-05] Estimator and adjustment code imports no network, downloader, FFmpeg, Core Audio, or ScreenCaptureKit APIs."
  artifacts:
    - path: Sources/FeatureBPMTapper/TempoEstimator.swift
      provides: "Pure Swift tempo estimation from tap timestamps with pause and outlier guardrails"
      min_lines: 90
      contains: "TempoEstimator"
    - path: Sources/FeatureBPMTapper/BPMAdjustment.swift
      provides: "Original, half-time, and double-time adjustment domain"
      min_lines: 25
      contains: "halfTime"
    - path: Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift
      provides: "Estimator tests for first estimate, recent interval averaging, long pause reset, and outlier handling"
      min_lines: 80
      contains: "testIgnoresObviousOutlier"
  key_links:
    - from: Package.swift
      to: Sources/FeatureBPMTapper/TempoEstimator.swift
      via: "SwiftPM target named FeatureBPMTapper includes the source file"
      pattern: "FeatureBPMTapper"
    - from: Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift
      to: Sources/FeatureBPMTapper/TempoEstimator.swift
      via: "tests import FeatureBPMTapper and call TempoEstimator.tap(at:)"
      pattern: "TempoEstimator"
    - from: Tests/FeatureBPMTapperTests/BPMAdjustmentTests.swift
      to: Sources/FeatureBPMTapper/BPMAdjustment.swift
      via: "tests verify original, half-time, and double-time output values"
      pattern: "BPMAdjustment"
---

<objective>
Create the BPM tapper domain module and focused tests for tempo calculation.

Purpose: Phase 2 should start with deterministic tap math before UI work. This plan creates a separate `FeatureBPMTapper` target, implements a pure Swift estimator with recent-interval averaging, pause reset, and outlier handling, and covers adjustment math for original, half-time, and double-time display values.

Output: `FeatureBPMTapper` SwiftPM target, `FeatureBPMTapperTests`, `TempoEstimator`, `BPMAdjustment`, and tests that prove the BPM calculation behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bpm-tapper/02-CONTEXT.md
@.planning/phases/02-bpm-tapper/02-RESEARCH.md
@.planning/phases/02-bpm-tapper/02-VALIDATION.md
@.planning/phases/02-bpm-tapper/02-PATTERNS.md
@Package.swift
</context>

<threat_model>
Trust boundaries:
- Tap timestamps are local user input and may include accidental double-clicks, missed beats, or long pauses.
- The estimator is pure local logic with no file, network, helper-tool, or OS permission boundary.
- Tests are the main safety boundary before UI consumes the estimator.

STRIDE register:
- threat_id: T-02-DOMAIN
  category: Tampering
  component: TempoEstimator
  disposition: mitigate
  severity: low
  mitigation_plan: Bound interval acceptance with minimum/maximum interval checks and outlier tolerance tests.
- threat_id: T-02-STALE
  category: Denial of Service
  component: TempoEstimator
  disposition: mitigate
  severity: low
  mitigation_plan: Reset the current run after a pause threshold so stale intervals cannot poison the next session.
- threat_id: T-02-OFFLINE
  category: Information Disclosure
  component: FeatureBPMTapper target
  disposition: mitigate
  severity: low
  mitigation_plan: Keep the target free of network/helper/audio-capture imports and verify with a source scan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add FeatureBPMTapper package and test targets</name>
  <files>Package.swift</files>
  <read_first>Package.swift, .planning/phases/02-bpm-tapper/02-CONTEXT.md, .planning/phases/02-bpm-tapper/02-PATTERNS.md</read_first>
  <action>Update `Package.swift` by adding a library product exactly `.library(name: "FeatureBPMTapper", targets: ["FeatureBPMTapper"])`. Add a target exactly `.target(name: "FeatureBPMTapper", dependencies: ["AppCore"])`. Add a test target exactly `.testTarget(name: "FeatureBPMTapperTests", dependencies: ["FeatureBPMTapper"])`. Leave the `OutsideCubaseHub` executable target dependency list unchanged in this task; Plan 02-02 will add the executable dependency when the feature view is registered. Preserve `.macOS("14.2")` and the existing `AppCore` and `AppCoreTests` targets.</action>
  <verify>rg -n "FeatureBPMTapper|FeatureBPMTapperTests" Package.swift</verify>
  <acceptance_criteria>
    - `Package.swift` contains `.library(`
    - `Package.swift` contains `name: "FeatureBPMTapper"`
    - `Package.swift` contains `.target(`
    - `Package.swift` contains `dependencies: ["AppCore"]`
    - `Package.swift` contains `.testTarget(`
    - `Package.swift` contains `FeatureBPMTapperTests`
    - `rg -n "FeatureBPMTapper|FeatureBPMTapperTests" Package.swift` returns matches for the library target and test target
  </acceptance_criteria>
  <done>SwiftPM knows about the BPM feature module and its test target.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TempoEstimator with recent interval averaging</name>
  <files>Sources/FeatureBPMTapper/TempoEstimator.swift, Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift</files>
  <read_first>Sources/AppCore/Jobs/Job.swift, Tests/AppCoreTests/SettingsStoreTests.swift, .planning/phases/02-bpm-tapper/02-CONTEXT.md, .planning/phases/02-bpm-tapper/02-RESEARCH.md, .planning/phases/02-bpm-tapper/02-VALIDATION.md</read_first>
  <action>Create `TempoEstimatorConfiguration`, `TempoEstimatorStatus`, `TempoEstimate`, and `TempoEstimator` in `Sources/FeatureBPMTapper/TempoEstimator.swift`. Use concrete defaults: `recentIntervalLimit = 4`, `pauseResetThreshold = 2.5`, `minimumInterval = 0.24`, `maximumInterval = 2.0`, and `outlierTolerance = 0.35`. Implement `mutating func tap(at timestamp: TimeInterval) -> TempoEstimate` and `mutating func reset()`. The first tap returns no BPM and status `.waitingForSecondTap`; the second tap returns BPM `60 / interval` and status `.firstEstimate`; once at least 4 accepted intervals are available, status becomes `.stableEstimate`; intervals longer than `pauseResetThreshold` reset the run and return status `.longPauseReset`; intervals outside min/max or beyond tolerance from the recent average return status `.outlierIgnored` while preserving the prior BPM. Compute BPM from the average of the last `recentIntervalLimit` accepted intervals. Add XCTest cases named exactly `testFirstTapWaitsForSecondTap`, `testSecondTapProducesFirstEstimate`, `testUsesLastFourAcceptedIntervals`, `testLongPauseStartsFreshRun`, and `testIgnoresObviousOutlier`.</action>
  <verify>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter TempoEstimatorTests</verify>
  <acceptance_criteria>
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `struct TempoEstimatorConfiguration`
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `recentIntervalLimit = 4`
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `pauseResetThreshold = 2.5`
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `mutating func tap(at timestamp: TimeInterval)`
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `case longPauseReset`
    - `Sources/FeatureBPMTapper/TempoEstimator.swift` contains `case outlierIgnored`
    - `Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift` contains `testUsesLastFourAcceptedIntervals`
    - `Tests/FeatureBPMTapperTests/TempoEstimatorTests.swift` contains `testIgnoresObviousOutlier`
    - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter TempoEstimatorTests` exits 0
  </acceptance_criteria>
  <done>Tap math is deterministic, tested, and independent from SwiftUI.</done>
</task>

<task type="auto">
  <name>Task 3: Implement BPMAdjustment and offline source scan</name>
  <files>Sources/FeatureBPMTapper/BPMAdjustment.swift, Tests/FeatureBPMTapperTests/BPMAdjustmentTests.swift</files>
  <read_first>Sources/FeatureBPMTapper/TempoEstimator.swift, .planning/phases/02-bpm-tapper/02-CONTEXT.md, .planning/phases/02-bpm-tapper/02-UI-SPEC.md</read_first>
  <action>Create `BPMAdjustment` as a `String`, `CaseIterable`, `Codable`, `Sendable`, `Identifiable` enum with cases exactly `original`, `halfTime`, and `doubleTime`. Add `var id: String { rawValue }`, `var displayName: String` returning `Original`, `Half-Time`, and `Double-Time`, and `func apply(to bpm: Double) -> Double` returning `bpm`, `bpm / 2.0`, or `bpm * 2.0`. Add XCTest cases named exactly `testOriginalKeepsBPM`, `testHalfTimeDividesBPM`, `testDoubleTimeMultipliesBPM`, and `testDisplayNamesMatchUISpec`. Run a source scan that confirms `Sources/FeatureBPMTapper` does not contain `URLSession`, `Network`, `yt-dlp`, `FFmpeg`, `CoreAudio`, or `ScreenCaptureKit`.</action>
  <verify>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMAdjustmentTests && ! rg -n "URLSession|Network|yt-dlp|FFmpeg|CoreAudio|ScreenCaptureKit" Sources/FeatureBPMTapper</verify>
  <acceptance_criteria>
    - `Sources/FeatureBPMTapper/BPMAdjustment.swift` contains `enum BPMAdjustment`
    - `Sources/FeatureBPMTapper/BPMAdjustment.swift` contains `case original`
    - `Sources/FeatureBPMTapper/BPMAdjustment.swift` contains `case halfTime`
    - `Sources/FeatureBPMTapper/BPMAdjustment.swift` contains `case doubleTime`
    - `Sources/FeatureBPMTapper/BPMAdjustment.swift` contains `func apply(to bpm: Double) -> Double`
    - `Tests/FeatureBPMTapperTests/BPMAdjustmentTests.swift` contains `testHalfTimeDividesBPM`
    - `Tests/FeatureBPMTapperTests/BPMAdjustmentTests.swift` contains `testDisplayNamesMatchUISpec`
    - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMAdjustmentTests` exits 0
    - `rg -n "URLSession|Network|yt-dlp|FFmpeg|CoreAudio|ScreenCaptureKit" Sources/FeatureBPMTapper` returns no matches
  </acceptance_criteria>
  <done>Adjustment modes are test-covered and the BPM feature remains offline-only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter TempoEstimatorTests` exits 0.
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --filter BPMAdjustmentTests` exits 0.
- [ ] `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test` exits 0.
- [ ] `rg -n "URLSession|Network|yt-dlp|FFmpeg|CoreAudio|ScreenCaptureKit" Sources/FeatureBPMTapper` returns no matches.
- [ ] `Package.swift` defines `FeatureBPMTapper` and `FeatureBPMTapperTests`.
</verification>

<success_criteria>
- All tasks completed.
- BPM-02 estimator behavior is deterministic and covered by XCTest.
- BPM-03 adjustment math exists for later UI actions.
- BPM-05 offline behavior is protected by source scan.
- D-03, D-05, D-06, D-07, D-08, and D-09 are visible in source or tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bpm-tapper/02-01-SUMMARY.md`.
</output>
