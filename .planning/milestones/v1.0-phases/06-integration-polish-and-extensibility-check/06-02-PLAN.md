---
phase: 06-integration-polish-and-extensibility-check
plan: "06-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/FeatureBPMTapper/BPMTapperView.swift
  - Sources/FeatureAudioRecorder/AudioRecorderView.swift
  - Sources/FeatureDownloader/DownloaderView.swift
  - Sources/FeatureAudioConverter/AudioConverterView.swift
  - Sources/OutsideCubaseHub/AppShell/AppShellView.swift
  - Sources/OutsideCubaseHub/AppMenu.swift (new)
autonomous: false
requirements: ["UX-04"]
user_setup: []
must_haves:
  truths:
    - "[UX-04] BPM Tapper: Space = tap, Escape = reset, Cmd+C = copy BPM, Cmd+S = save BPM (when tapped surface focused)."
    - "[UX-04] Audio Recorder: Space = start/stop (when recorder focused), Escape = cancel (when recording)."
    - "[UX-04] Downloader: Cmd+Enter = start download (when URL field and downloader focused), Escape = clear input."
    - "[UX-04] Audio Converter: Cmd+Shift+V = start conversion (when files queued and converter focused), Escape = cancel."
    - "[UX-04] App menu includes: About, Hide, Quit, Settings (Cmd+,), standard Edit/Window menus."
  artifacts: []
---

<objective>
Add or verify context-sensitive keyboard shortcuts and ensure the macOS app menu is complete and polished. Shortcuts fire only when the corresponding tool view is active/focused. The menu follows macOS conventions.

Purpose: UX-04 requires keyboard shortcuts for frequent actions. This plan implements the shortcut table from 06-CONTEXT.md and adds the application menu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cmd+C and Cmd+S shortcuts to BPMTapperView</name>
  <files>Sources/FeatureBPMTapper/BPMTapperView.swift</files>
  <read_first>Sources/FeatureBPMTapper/BPMTapperView.swift, .planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md</read_first>
  <action>
Read `BPMTapperView.swift` to find the tap surface and existing `.onKeyPress(.space)` and `.onKeyPress(.escape)` handlers. The view should already have a `@FocusedValue` for `bpmFocused`. Add `.onKeyPress(.leftCommand)` + `.onKeyPress(.c)` handler when `bpmFocused == true` to copy the displayed BPM to clipboard. Add `.onKeyPress(.leftCommand)` + `.onKeyPress(.s)` handler when `bpmFocused == true` and a BPM is showing (non-nil) to save the BPM. The copy action uses `NSPasteboard.general.string = bpmValue` and the save action calls the existing save logic in the view model.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `BPMTapperView.swift` has `.onKeyPress` handler for Cmd+C that copies displayed BPM
    - `BPMTapperView.swift` has `.onKeyPress` handler for Cmd+S that saves displayed BPM
    - Both handlers fire only when tap surface is focused
    - `swift build` exits 0
  </acceptance_criteria>
  <done>Cmd+C and Cmd+S shortcuts are wired in BPMTapperView.</done>
</task>

<task type="auto">
  <name>Task 2: Verify AudioRecorderView Space and Escape shortcuts</name>
  <files>Sources/FeatureAudioRecorder/AudioRecorderView.swift</files>
  <read_first>Sources/FeatureAudioRecorder/AudioRecorderView.swift, .planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md</read_first>
  <action>
Read `AudioRecorderView.swift`. Verify that Space-based start/stop and Escape-based cancel are already implemented via `.onKeyPress` modifiers that fire only when `recorderFocused == true`. If they exist, confirm the implementation matches the spec: Space toggles start/stop, Escape cancels recording in progress. If missing, add them using the same pattern as BPMTapperView's focus + `.onKeyPress` approach.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `AudioRecorderView.swift` has Space-based start/stop shortcut via `.onKeyPress`
    - `AudioRecorderView.swift` has Escape-based cancel shortcut via `.onKeyPress`
    - Both shortcuts fire only when recorderFocused is true
    - `swift build` exits 0
  </acceptance_criteria>
  <done>Recorder Space and Escape shortcuts are confirmed working.</done>
</task>

<task type="auto">
  <name>Task 3: Add Cmd+Enter and Escape shortcuts to DownloaderView</name>
  <files>Sources/FeatureDownloader/DownloaderView.swift</files>
  <read_first>Sources/FeatureDownloader/DownloaderView.swift, .planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md</read_first>
  <action>
Read `DownloaderView.swift`. Add a `@FocusedValue` for `downloaderFocused` (Bool) similar to the recorder pattern. Add an `.onKeyPress` modifier for `Cmd+Enter` that starts the download when the URL field has content and `downloaderFocused == true`. Add an `.onKeyPress(.escape)` handler that clears the URL input field when it has content and `downloaderFocused == true`. The view should already have URL input binding.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `DownloaderView.swift` has `@FocusedValue` for `downloaderFocused`
    - `DownloaderView.swift` has `.onKeyPress` for Cmd+Enter to start download
    - `DownloaderView.swift` has `.onKeyPress(.escape)` to clear URL input
    - `swift build` exits 0
  </acceptance_criteria>
  <done>Downloader keyboard shortcuts (Cmd+Enter, Escape) are wired.</done>
</task>

<task type="auto">
  <name>Task 4: Add Cmd+Shift+V and Escape shortcuts to AudioConverterView</name>
  <files>Sources/FeatureAudioConverter/AudioConverterView.swift</files>
  <read_first>Sources/FeatureAudioConverter/AudioConverterView.swift, .planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md</read_first>
  <action>
Read `AudioConverterView.swift`. Add a `@FocusedValue` for `converterFocused` (Bool). Add an `.onKeyPress` modifier for `Cmd+Shift+V` that starts conversion when files are queued and `converterFocused == true`. Add `.onKeyPress(.escape)` that cancels/clears the active conversion when `converterFocused == true`. Use the same focus + `.onKeyPress` pattern.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `AudioConverterView.swift` has `@FocusedValue` for `converterFocused`
    - `AudioConverterView.swift` has `.onKeyPress` for Cmd+Shift+V to start conversion
    - `AudioConverterView.swift` has `.onKeyPress(.escape)` to cancel conversion
    - `swift build` exits 0
  </acceptance_criteria>
  <done>Converter keyboard shortcuts (Cmd+Shift+V, Escape) are wired.</done>
</task>

<task type="auto">
  <name>Task 5: Create AppMenu.swift with macOS app menu structure</name>
  <files>Sources/OutsideCubaseHub/AppMenu.swift (new), Sources/OutsideCubaseHub/OutsideCubaseHubApp.swift</files>
  <read_first>Sources/OutsideCubaseHub/OutsideCubaseHubApp.swift, .planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md</read_first>
  <action>
Create `AppMenu.swift` as a SwiftUI `AppMenu` that returns a `MenuBarAppearance` for the app. The menu bar should include:

1. **Outside Cubase Hub** menu: About Outside Cubase Hub, Hide Outside Cubase Hub (Cmd+H), Quit Outside Cubase Hub (Cmd+Q)
2. **File** menu: Settings (Cmd+,), Close Window (Cmd+W)
3. **Edit** menu: Undo (Cmd+Z), Cut (Cmd+X), Copy (Cmd+C), Paste (Cmd+V), Select All (Cmd+A)
4. **Window** menu: Minimize (Cmd+M), Zoom
5. **Help** menu: "Outside Cubase Hub Help"

Wire this into `OutsideCubaseHubApp.swift` via the `.menuBarExtra` or `.commands` view modifier. The app should NOT have a custom toolbar — keyboard shortcuts and sidebar navigation are the primary interaction model.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `Sources/OutsideCubaseHub/AppMenu.swift` exists with full menu structure
    - About, Hide, Quit present in app menu
    - Settings (Cmd+,), Close Window (Cmd+W) present in File menu
    - Standard Edit menu items present
    - Window menu with Minimize present
    - Help menu present
    - `swift build` exits 0
  </acceptance_criteria>
  <done>App menu is complete and polished.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0 across all targets
- [ ] All keyboard shortcuts in the shortcut table (06-CONTEXT.md) are implemented
- [ ] All shortcuts are context-sensitive — they only fire when the corresponding tool is focused
- [ ] App menu includes all required items from the menu polish section
- [ ] No shortcut conflicts with standard macOS behavior (Tab, Arrow keys, etc.)
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- Context-sensitive shortcuts exist for BPM (Space, Escape, Cmd+C, Cmd+S), Recorder (Space, Escape), Downloader (Cmd+Enter, Escape), Converter (Cmd+Shift+V, Escape).
- App menu is complete and follows macOS conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-polish-and-extensibility-check/06-02-SUMMARY.md`.
</output>
