---
phase: 06-integration-polish-and-extensibility-check
plan: "06-04"
type: execute
wave: 1
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - Sources/FeaturePlaceholder/FeaturePlaceholder.swift (new)
  - Sources/OutsideCubaseHub/AppComposition.swift
  - Sources/AppCore/Features/ToolFeature.swift
  - Sources/AppCore/Features/ToolRegistry.swift
autonomous: false
requirements: ["UX-03", "UX-04", "FND-02"]
user_setup: []
must_haves:
  truths:
    - "[FND-02] A placeholder feature can be registered by adding it to the features array in AppComposition.swift — no existing feature internals need modification."
    - "[FND-02] The placeholder appears in the sidebar with correct metadata (id, displayName, shortLabel, systemImage)."
    - "[FND-02] Clicking the placeholder in the sidebar shows its view without affecting existing features."
    - "[FND-02] Removing the placeholder reverts AppComposition.swift to its prior state with no lingering effects."
  artifacts: []
---

<objective>
Verify the architecture is still extensible by adding and removing a minimal placeholder feature through the registry. Also perform codebase cleanup: remove dead code, verify imports, and check registry pattern integrity.

Purpose: FND-02 requires that a new tool can be added without editing existing feature internals. This plan proves and verifies that extensibility contract, then removes the placeholder cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-integration-polish-and-extensibility-check/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-foundation-and-tool-architecture/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeaturePlaceholder module with registration</name>
  <files>Sources/FeaturePlaceholder/FeaturePlaceholder.swift (new), Sources/FeaturePlaceholder/.gitkeep (new)</files>
  <read_first>Sources/AppCore/Features/ToolFeature.swift, Sources/AppCore/Features/ToolRegistry.swift, Sources/OutsideCubaseHub/AppComposition.swift, AGENTS.md</read_first>
  <action>
Create `Sources/FeaturePlaceholder/FeaturePlaceholder.swift` as a minimal placeholder feature. The module structure should mirror existing feature modules (e.g., `FeatureBPMTapper`):

```swift
import AppCore
import SwiftUI

public struct PlaceholderFeature: ToolFeature {
    public let metadata: ToolMetadata

    public init() {
        self.metadata = ToolMetadata(
            id: "placeholder",
            displayName: "Placeholder Tool",
            shortLabel: "Placeholder",
            systemImage: "questionmark.circle",
            capabilities: [.producesFiles]
        )
    }

    @MainActor
    public func makeView(context: ToolContext) -> AnyView {
        AnyView(
            VStack(spacing: 16) {
                Image(systemName: "questionmark.circle")
                    .font(.system(size: 40))
                    .foregroundStyle(.secondary)
                Text("Placeholder: tool not yet implemented")
                    .font(.system(size: 14))
                    .foregroundStyle(.secondary)
                Text("This feature was added through the registry with no changes to existing feature code.")
                    .font(.system(size: 12))
                    .foregroundStyle(.tertiary)
                    .multilineTextAlignment(.center)
            }
            .frame(maxWidth: 680, alignment: .center)
            .padding()
        )
    }
}
```

Also create `Sources/FeaturePlaceholder/.gitkeep` to ensure the directory is tracked.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `Sources/FeaturePlaceholder/FeaturePlaceholder.swift` exists with `PlaceholderFeature` struct
    - `PlaceholderFeature` conforms to `ToolFeature` protocol
    - Metadata has id="placeholder", displayName="Placeholder Tool", shortLabel="Placeholder", systemImage="questionmark.circle"
    - Capability `.producesFiles` is set
    - `swift build` exits 0
  </acceptance_criteria>
  <done>FeaturePlaceholder module exists.</done>
</task>

<task type="auto">
  <name>Task 2: Register PlaceholderFeature in AppComposition and verify it appears</name>
  <files>Sources/OutsideCubaseHub/AppComposition.swift</files>
  <read_first>Sources/OutsideCubaseHub/AppComposition.swift, Sources/FeaturePlaceholder/FeaturePlaceholder.swift</read_first>
  <action>
Read `AppComposition.swift`. Add `import FeaturePlaceholder` at the top and add `PlaceholderFeature()` to the features array. The entire change should be:

1. Add `import FeaturePlaceholder` after the existing import lines
2. Add `PlaceholderFeature()` as a new entry in the features array

No other changes to AppComposition, ToolRegistry, or any other feature should be needed. Verify by building.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `AppComposition.swift` has `import FeaturePlaceholder`
    - `AppComposition.swift` has `PlaceholderFeature()` in the features array
    - `swift build` exits 0
    - Registry now has 6 features (5 existing + placeholder)
  </acceptance_criteria>
  <done>Placeholder is registered and app builds successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Verify placeholder appears in sidebar and shows its view</name>
  <files>Sources/OutsideCubaseHub/AppShell/ToolSidebarView.swift</files>
  <read_first>Sources/OutsideCubaseHub/AppShell/ToolSidebarView.swift, Sources/FeaturePlaceholder/FeaturePlaceholder.swift</read_first>
  <action>
Read `ToolSidebarView.swift` to confirm it renders sidebar rows from the registry's features array. Since the sidebar already iterates over `registry.features` and renders each feature's metadata, the placeholder should automatically appear in the sidebar with the correct icon and shortLabel once registered. No code changes should be needed in the sidebar itself. Verify the placeholder metadata in `FeaturePlaceholder` uses the correct fields (`id`, `displayName`, `shortLabel`, `systemImage`) that the sidebar expects.
</action>
  <verify>swift build 2>&1 | head -30 && echo "Sidebar renders all registry features — placeholder auto-appears"</verify>
  <acceptance_criteria>
    - ToolSidebarView renders features from registry (existing behavior)
    - Placeholder appears in sidebar with icon "questionmark.circle" and label "Placeholder"
    - Clicking placeholder shows the placeholder view
    - No sidebar code modifications were needed
  </acceptance_criteria>
  <done>Placeholder auto-appears in sidebar through existing registry rendering.</done>
</task>

<task type="auto">
  <name>Task 4: Remove PlaceholderFeature from AppComposition (cleanup)</name>
  <files>Sources/OutsideCubaseHub/AppComposition.swift, Sources/FeaturePlaceholder/FeaturePlaceholder.swift</files>
  <read_first>Sources/OutsideCubaseHub/AppComposition.swift, Sources/FeaturePlaceholder/FeaturePlaceholder.swift</read_first>
  <action>
Remove the PlaceholderFeature registration from AppComposition.swift (remove the `PlaceholderFeature()` entry and the `import FeaturePlaceholder` line). Delete `Sources/FeaturePlaceholder/FeaturePlaceholder.swift` and `Sources/FeaturePlaceholder/.gitkeep`. The module directory `Sources/FeaturePlaceholder/` can be removed entirely. Verify the build still passes after removal.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - AppComposition.swift no longer imports FeaturePlaceholder
    - FeaturePlaceholder directory removed from Sources/
    - `swift build` exits 0
    - Registry has 5 features (original set restored)
  </acceptance_criteria>
  <done>Placeholder removed cleanly; app returns to pre-placeholder state.</done>
</task>

<task type="auto">
  <name>Task 5: Registry and codebase cleanup audit</name>
  <files>Sources/AppCore/Features/ToolFeature.swift, Sources/AppCore/Features/ToolRegistry.swift, Sources/AppCore/Jobs/Job.swift, Sources/OutsideCubaseHub/AppComposition.swift</files>
  <read_first>Sources/AppCore/Features/ToolFeature.swift, Sources/AppCore/Features/ToolRegistry.swift, Sources/AppCore/Jobs/Job.swift</read_first>
  <action>
Perform a cleanup audit:
1. Check all `import` statements in each feature module — verify each imported module is actually used (no dead imports)
2. Verify no feature directly instantiates another feature's internals (e.g., no feature should do `AudioConverterFeature()` or `DownloaderFeature()` directly — all cross-feature coupling goes through the shared ToolContext/registry)
3. Verify `JobLogEntry` messages are concise and useful — not raw stderr dumps with no context (grep for large unformatted error patterns)
4. Check `DevToolFeature` is still appropriate as a dev placeholder or consolidate if redundant
5. Verify `ToolCapability` has `.producesFiles` and `.runsJobs` cases and they are used correctly

Report findings but do not make changes unless you find clear bugs — this is a cleanup audit, not a refactor.
</action>
  <verify>swift build 2>&1 | head -30</verify>
  <acceptance_criteria>
    - All imports are used in their respective modules
    - No feature instantiates another feature's internals directly
    - JobLogEntry messages are concise (grep-able for usefulness)
    - DevToolFeature remains appropriate
    - ToolCapability cases are correct
    - `swift build` exits 0
  </acceptance_criteria>
  <done>Cleanup audit complete. Registry integrity verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` exits 0 across all targets (before and after placeholder removal)
- [ ] Placeholder was registered, appeared in sidebar, showed its view
- [ ] Placeholder was removed cleanly — AppComposition restored to original state
- [ ] No dead imports found
- [ ] No cross-feature coupling found
- [ ] JobLogEntry messages are useful and concise
- [ ] Registry pattern (ToolFeature + ToolRegistry) is clean and intact
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- FND-02 extensibility contract is proven: placeholder added with only a registration line change in AppComposition.
- Placeholder removed cleanly with no lingering effects.
- Codebase cleanup audit complete.
- Registry pattern is verified clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-polish-and-extensibility-check/06-04-SUMMARY.md`.
</output>
