---
phase: "04"
plan: "04-04"
type: "execute"
wave: "4"
depends_on:
  - "04-03"
files_modified:
  - "Tests/FeatureAudioRecorderTests/RecorderIntegrationTests.swift"
autonomous: false
requirements:
  - "REC-01"
  - "REC-02"
  - "REC-03"
  - "REC-04"
  - "REC-05"
  - "REC-06"
---

<objective>
Manual verification against real system audio. Run the app, record actual system audio (music, video playback, etc.), confirm the WAV file is real and playable, and verify all success criteria are met.
</objective>

<tasks>

<task>
<type>execute</type>
<read_first>
- ".planning/phases/04-internal-audio-recorder/04-CONTEXT.md"
- ".planning/ROADMAP.md"
</read_first>
<action>
Create `Tests/FeatureAudioRecorderTests/RecorderIntegrationTests.swift` for manual verification tests that require the full system.

These tests document the manual verification steps and can be run as a checklist:

```swift
import XCTest
@testable import FeatureAudioRecorder

final class RecorderIntegrationTests: XCTestCase {
    func testRecorderFeatureAppearsInToolRegistry() async throws {
        // Verify audio-recorder tool is registered and appears in tool list
        // Run: swift build && ./OutsideCubaseHub --list-tools 2>/dev/null | grep audio-recorder
    }

    func testRecordingCapturesRealSystemAudio() async throws {
        // Manual test: play music/video, start recording, stop after 10s, verify file has audio content
        // Use: try AVAudioFile(forReading: recordedURL) and check that frameLength > 0
    }

    func testOutputFileIsPlayableInQuickTime() async throws {
        // Manual test: record 5 seconds of audio, verify file opens in QuickTime Player
        // Command: open -a QuickTime\ Player /path/to/recording.wav
    }

    func testOutputFileHasCorrectFormat() async throws {
        // After recording: use AVAudioFile to read and verify:
        // - sampleRate == 44100
        // - bitDepth == 24
        // - channelCount == 2 (or 1 for mono source with preserveMonoStereo)
    }

    func testRecordingAppearsInOutputInbox() async throws {
        // Record audio, verify OutputInboxItem is created with:
        // - sourceToolID == "audio-recorder"
        // - status == .available
        // - metadata contains duration and audio specs
    }

    func testRevealOpensCorrectFolder() async throws {
        // After recording, call fileActions.revealInFinder(url:) and verify Finder opens at output folder
        // Use AppleScript: osascript -e 'tell app "Finder" to get POSIX file outputURL.path as alias'
    }

    func testPermissionPromptAppearsOnFirstUse() async throws {
        // Fresh install test: launch app, click Start Recording, verify system permission dialog appears
        // Check: the app does NOT silently fail — permission state is shown in UI
    }

    func testIncompatibleMacOSShowsMessage() async throws {
        // Manual: check that if macOS < 14.2, the incompatibleMacOS state shows correct version info
        // Use: sw_vers to get current version and compare
    }

    func testMaxDurationAutoStop() async throws {
        // Set max duration to 5 seconds, record, verify auto-stop at exactly 5 seconds
        // Check: elapsed time shown as ~5.0 seconds
    }

    func testFilenameOverrideRoundTrip() async throws {
        // Set filename override to "My Test Recording.wav", record, verify output file has that exact name
        // Use: FileManager.default.fileExists(atPath: outputDir.appendingPathComponent("My Test Recording.wav").path)
    }

    func testDragOutHandoffWorks() async throws {
        // After recording, simulate drag-out: get NSItemProvider from OutputHandoff for the recorded file
        // Verify NSItemProvider has the correct UTI (public.wav) and fileURL
    }
}
```

These are integration/manual tests. The test file documents the expected manual verification steps and can serve as a test checklist that the developer runs manually and marks as pass/fail.
</action>
<acceptance_criteria>
- `ls Tests/FeatureAudioRecorderTests/RecorderIntegrationTests.swift` exists
- At least 10 test cases covering all success criteria
- All tests are documented with clear manual verification steps
</acceptance_criteria>
<verify>
Review the test file to confirm it covers all success criteria from the ROADMAP.
</verify>
</task>

<task>
<type>execute</type>
<read_first>
- "Sources/FeatureAudioRecorder/AudioRecorderView.swift"
</read_first>
<action>
Do a final review pass against all success criteria from the ROADMAP:

**Success Criteria for Phase 4:**
1. App gates recording by macOS compatibility and displays clear permission guidance
   - Verify: `isCompatibleMacOS()` gate is in place in the view model
   - Verify: `permissionNeeded` state shows "Grant Permission" button
   - Verify: Info.plist has `NSAudioCaptureUsageDescription`

2. User can start and stop recording with obvious active-state feedback
   - Verify: Start/Stop button label changes
   - Verify: Pulsing red recording indicator visible when recording
   - Verify: Spacebar works when view is focused
   - Verify: Elapsed time counts up

3. Recording captures internal/system audio and writes a playable WAV PCM file
   - Verify: WAVOutputVerifier passes on recorded output
   - Verify: Recorded file plays in QuickTime/VLC
   - Verify: Format matches project preset (44100 Hz, 24-bit, stereo/preserve-mono)

4. Permission/API/output errors are distinguishable and recoverable
   - Verify: permissionDenied shows "Open System Settings" button
   - Verify: apiError shows retry button with error message
   - Verify: writeError shows disk space check
   - Verify: verificationFailed shows retry button

5. Recorded file appears in output inbox and can be revealed or dragged into Cubase
   - Verify: OutputInboxItem is created after successful recording
   - Verify: "Reveal" button opens Finder at output location
   - Verify: "Open" button launches QuickTime/VLC with file
   - Verify: OutputHandoff is used for drag-out

Create a checklist file at `.planning/phases/04-internal-audio-recorder/04-VERIFICATION.md` that enumerates each success criterion and the exact verification steps.
</action>
<acceptance_criteria>
- `ls .planning/phases/04-internal-audio-recorder/04-VERIFICATION.md` exists
- The file has all 5 success criteria from ROADMAP with verification steps
</acceptance_criteria>
<verify>
Confirm the verification checklist covers all criteria. The checklist serves as the test plan for manual UAT.
</verify>
</task>

</tasks>

<verification>
1. Build succeeds: `swift build`
2. All unit tests pass: `swift test --filter FeatureAudioRecorder`
3. Integration test file exists with all manual verification steps documented
4. Verification checklist covers all 5 success criteria from ROADMAP
</verification>

<must_haves>
- [ ] `RecorderIntegrationTests.swift` with 10+ documented verification steps covering all success criteria
- [ ] `04-VERIFICATION.md` checklist matching ROADMAP success criteria
- [ ] Manual verification completed: real system audio captured, WAV is playable, file appears in inbox
</must_haves>

---
*Plan: 04-04*
*Wave: 4*
*Depends on: 04-03*
*Status: Ready for execution*