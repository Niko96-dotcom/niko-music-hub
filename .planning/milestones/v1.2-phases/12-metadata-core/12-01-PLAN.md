---
phase: "12"
plan: "01"
wave: 1
checkpoints: ["CP-03", "CP-04", "CP-05"]
---

# Plan 12-01: Metadata store, merge, onboarding

## Tasks

1. Add `SongUserMetadata`, `PreviewSelectionMode`, `SongUserMetadataStoring`, SQLite table + store.
2. Add `ArchiveMetadataMerger` and extend `Song` with user metadata fields + `effectiveDisplayTitle`.
3. Extend `MusicSearchMatcher` for aliases and app notes.
4. Wire metadata store in `AppComposition` / `ArchiveBrowserViewModel` (load/merge/save).
5. Add `archiveOnboardingCompleted` to `AppSettings`; `ArchiveFirstRunView` + onboarding gate.
6. Tests: metadata roundtrip, merge virtual title, search alias/note, onboarding flag.

## Verification

- `./script/ci.sh`
