---
phase: "15"
plan: "01"
status: done
---

# Plan 15-01: Persistence + domain + merger

- Extend `song_metadata` columns (migration via PRAGMA).
- Add `Collaborator`, `CollaboratorStoring`, `SQLiteCollaboratorStore`.
- Extend `Song`, `SongUserMetadata`, `ArchiveMetadataMerger`, `ArchiveSmartShelf`.
