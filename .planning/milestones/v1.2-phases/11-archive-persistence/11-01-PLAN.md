---
phase: "11"
plan: "01"
wave: 1
checkpoints: ["CP-01"]
---

# Plan 11-01: SQLite archive index snapshot

## Tasks

1. Add `ArchiveIndexSnapshot` + `ArchiveIndexStoring` protocol in `NikoMusicCore`.
2. Implement `SQLiteArchiveIndexStore` (libsqlite3, single-row upsert, App Support path).
3. Wire store in `AppComposition`; inject into `ArchiveBrowserViewModel`.
4. On init: `loadCachedIndexIfAvailable()` after roots load.
5. On successful scan: `saveCachedIndex(from: ScanResult)`.
6. Tests: roundtrip save/load; root mismatch skips load.

## Verification

- `swift test` / `./script/ci.sh`
