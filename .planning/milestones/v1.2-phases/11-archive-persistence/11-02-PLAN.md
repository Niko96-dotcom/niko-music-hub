---
phase: "11"
plan: "02"
wave: 2
checkpoints: ["CP-02"]
depends_on: ["11-01"]
---

# Plan 11-02: FSEvents debounced rescan

## Tasks

1. Add `ArchiveRootWatching` protocol + `FSEventsArchiveRootWatcher` in `AppCore`.
2. Start/stop streams when archive roots change in view model.
3. Debounce events (~2s); call existing `scan()` on main actor.
4. Tests: mock watcher invokes callback; no real FSEvents in CI.

## Verification

- `./script/ci.sh`
