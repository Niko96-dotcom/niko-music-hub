---
phase: "08"
plan: "01"
wave: 1
requirements: ["REC-01", "REC-02", "REC-03", "REC-05"]
---

# Plan 08-01: Core Audio process tap engine

## Tasks

1. Add `SystemAudioProcessTapSession.swift` — create/destroy tap + aggregate device, IO proc, format read, buffer → converter → writer.
2. Refactor `CoreAudioTapAdapter` to drive session; remove synthetic random levels and `writeSilenceWAV`.
3. macOS 14.2+ gate in `isCompatibleMacOS()` and incompatible error paths.
4. Add `NSAudioCaptureUsageDescription` to `script/build_and_run.sh` Info.plist template.

## Verification

- `swift test --filter CoreAudioTapAdapterTests` (skip or pass with permission).
- `swift test --filter RecorderIntegrationTests` (skip or pass with permission).
