---
phase: 20-shell-navigation
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift
  - Tests/AppCoreTests/OutputInboxStoreTests.swift
autonomous: true
requirements:
  - SH-03
must_haves:
  truths:
    - "Inbox title row shows Output + folder button; path on second line; no Output folder label"
    - "Empty state is vertically centered with icon and copy per §6.2"
    - "Output cards are horizontal with extension icon, hover-only drag grip, context menu, single-click reveal"
    - "No weak Divider between header and list — section spacing only"
  artifacts:
    - path: Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift
      provides: "Redesigned output inbox inspector"
  key_links:
    - from: itemCard
      to: context.fileActions.revealInFinder
      via: "onTapGesture or Button + contextMenu"
      pattern: "revealInFinder"
    - from: itemRow
      to: OutputHandoff.dragFileURL
      via: "onDrag when file exists"
      pattern: "onDrag"
---

<objective>
Redesign the output inbox inspector per UI-REDESIGN-PLAN §6 and decisions S-09–S-12, including interaction changes and test updates.

Purpose: SH-03 — inbox matches hub glass language and spec interactions (S-11 + discretion: single-click reveal).
Output: Updated `OutputInboxInspectorView` + adjusted source-contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shell-navigation/20-CONTEXT.md
@docs/UI-REDESIGN-PLAN.md
@Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift
@Tests/AppCoreTests/OutputInboxStoreTests.swift

<interfaces>
`OutputHandoff.isRevealable`, `OutputHandoff.dragFileURL`, `context.fileActions.revealInFinder`, `context.fileActions.open` (verify open exists on FileActions — use context menu "Open" only if API exists).

Existing test `testOutputInboxInspectorSourceContainsRevealAndDragHandoff` asserts strings in source — update expectations when removing inline Finder button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header layout and section spacing</name>
  <files>Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift</files>
  <action>
    Per S-09, S-12, spec §6.1, §6.4:
    - Title `Text("Output")` — size 14, semibold, rounded design (use `HubDesignSystem.Typography.sectionTitle()` if it matches 14pt semibold rounded)
    - Top row: `HStack` with title + `Spacer` + `HubIconButton` folder picker (same action as today)
    - Second line: path with size 10, `.truncationMode(.middle)`, `.foregroundStyle(.tertiary)`
    - Remove `Text("Output Inbox")`, `Text("Output folder")`, and `Divider().opacity(0.4)`
    - Root `VStack` spacing: use `HubDesignSystem.Spacing.section` (24) between header block and list/empty state
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift | rg -c 'Text\("Output"\)' | awk '{exit ($1>=1)?0:1}' && ! rg -q 'Output Inbox|Output folder|Divider\(\)\.opacity' Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift</automated>
  </verify>
  <done>Header matches §6.1; divider removed.</done>
</task>

<task type="auto">
  <name>Task 2: Centered empty state</name>
  <files>Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift</files>
  <action>
    Per S-10, spec §6.2:
    When `items.isEmpty`, use `Spacer()` + centered `VStack(spacing: 8)`:
    - `Image(systemName: "arrow.down.to.line")` size 24, `.foregroundStyle(.quaternary)`
    - Title "No outputs yet" — 13pt semibold
    - Body two lines per spec — 11pt, secondary, center aligned
    + `Spacer()` so block is vertically centered in remaining space (within padded panel).

    Remove old left-aligned empty copy.
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift | rg -c 'arrow\.down\.to\.line|No outputs yet' | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <done>Empty state matches §6.2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Horizontal cards, interactions, tests</name>
  <files>Sources/NikoMusicHub/AppShell/OutputInboxInspectorView.swift, Tests/AppCoreTests/OutputInboxStoreTests.swift</files>
  <behavior>
    - Cards use HStack: file-type icon (waveform/film/doc by extension), filename 12pt medium, status 10pt
    - No inline `HubIconButton` "Reveal in Finder" inside card
    - Source includes `contextMenu` with Reveal/Open, `onTapGesture` or equivalent single-click reveal per S-15
    - Drag grip visible on hover only; retains `onDrag` when drag URL exists
    - `testOutputInboxInspectorSourceContainsRevealAndDragHandoff` updated: still requires reveal + drag patterns; allows context-menu Reveal string instead of inline button label if needed
  </behavior>
  <action>
    Per S-11, spec §6.3:
    - Refactor `itemCard` to horizontal layout; padding 10; corner `HubDesignSystem.Radius.row`
    - `fileIcon(for: URL)` helper mapping extensions `.wav` → waveform, `.mp4` → film, default doc; frame 22×22, icon 14pt, secondary color
    - `@State` hover per row or shared hover ID; show `line.3.horizontal` grip only when hovered
    - `.contextMenu { Button("Reveal in Finder") ...; Button("Open") ... }` when revealable
    - Single-click on card: call `revealInFinder` when `OutputHandoff.isRevealable(item)` (per S-11 + discretion in CONTEXT)
    - List row spacing: `HubDesignSystem.Spacing.cardGap` (8) via list spacing or VStack between cards
    - Keep existing drag handoff and accessibility hints where still accurate

    Update `OutputInboxStoreTests.testOutputInboxInspectorSourceContainsRevealAndDragHandoff`:
    - Keep: `OutputHandoff.isRevealable`, `OutputHandoff.dragFileURL`, `NSItemProvider`, `.onDrag`, `contentShape`
    - Require `contextMenu` or `Reveal in Finder` in source (menu label OK)
    - Remove requirement for inline-only patterns if obsolete
  </action>
  <verify>
    <automated>swift test --filter testOutputInboxInspectorSourceContainsRevealAndDragHandoff && ./script/ci.sh</automated>
  </verify>
  <done>Cards match §6.3; tests and CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | Reveal/open only via existing FileActions on known output paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | I | Output paths displayed | accept | Paths already shown; truncation only |
| T-20-04 | E | revealInFinder / open | mitigate | Use existing AppCore FileActions; no new shell execution |
</threat_model>

<verification>
- `swift test --filter OutputInboxStoreTests`
- `./script/ci.sh`
</verification>

<success_criteria>
- SH-03 satisfied per ROADMAP success criteria #3
</success_criteria>

<output>
After completion, create `.planning/phases/20-shell-navigation/20-03-SUMMARY.md`
</output>
