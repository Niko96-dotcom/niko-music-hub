---
phase: 20-shell-navigation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - Sources/NikoMusicHub/AppShell/ToolSidebarView.swift
  - Sources/NikoMusicHub/AppShell/HelperToolsHealthStrip.swift
autonomous: true
requirements:
  - SH-02
must_haves:
  truths:
    - "Sidebar header shows app name and bundle version (not hardcoded v1.0 or Local tools)"
    - "Each tool row shows hierarchical 18×18 icon + 13pt label with 36px row height"
    - "Selected row uses hub accent label and accentTint/selectedStroke chrome"
    - "Health strip uses 7px dots and §5.4 typography/padding"
  artifacts:
    - path: Sources/NikoMusicHub/AppShell/ToolSidebarView.swift
      provides: "Icon+label navigation rows and bundle version header"
      contains: "CFBundleShortVersionString"
    - path: Sources/NikoMusicHub/AppShell/HelperToolsHealthStrip.swift
      provides: "Refined helper health strip"
  key_links:
    - from: ToolSidebarView tool Button
      to: hubSidebarNavRow
      via: "selection chrome"
      pattern: "hubSidebarNavRow"
---

<objective>
Redesign tool sidebar identity, navigation rows, and helper health strip per UI-REDESIGN-PLAN §5 and decisions S-06–S-08.

Purpose: SH-02 — sidebar reads as first-class hub chrome, not placeholder labels.
Output: Updated `ToolSidebarView` + `HelperToolsHealthStrip`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shell-navigation/20-CONTEXT.md
@docs/UI-REDESIGN-PLAN.md
@Sources/NikoMusicHub/AppShell/ToolSidebarView.swift
@Sources/NikoMusicHub/AppShell/HelperToolsHealthStrip.swift
@Sources/AppCore/Components/HubGlassChrome.swift

<interfaces>
`HubSidebarNavRow` already sets selected label `HubDesignSystem.Colors.accent` and unselected `.primary` — align row padding/height in ToolSidebarView, do not duplicate fill logic.

`HubDesignSystem.Size.statusDot` == 7 from Phase 19.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App identity header with bundle version</name>
  <files>Sources/NikoMusicHub/AppShell/ToolSidebarView.swift</files>
  <action>
    Per S-06 and spec §5.1:
    - Logo frame 26×26 (was 30×30)
    - App name: `.font(.system(size: 14, weight: .semibold, design: .rounded))`
    - Version line: read `Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String`, prefix with `"v"` when non-nil; font size 10, weight `.medium`, `.foregroundStyle(.tertiary)`
    - Remove `Text("Local tools")` entirely
    - Header padding: horizontal 14, top 14, bottom 8

    Add small private helper `appVersionLabel: String` if needed; never hardcode `"v1.0"`.
  </action>
  <verify>
    <automated>rg -q 'CFBundleShortVersionString' Sources/NikoMusicHub/AppShell/ToolSidebarView.swift && ! rg -q 'Local tools' Sources/NikoMusicHub/AppShell/ToolSidebarView.swift</automated>
  </verify>
  <done>Header matches §5.1; no "Local tools"; version from bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Tool rows and TOOLS section label</name>
  <files>Sources/NikoMusicHub/AppShell/ToolSidebarView.swift</files>
  <action>
    Per S-07 and spec §5.2–5.3:
    - Section label "Tools": `.foregroundStyle(.quaternary)`, `.padding(.bottom, 2)` (was 4)
    - Replace `Label(metadata.shortLabel, systemImage:)` with explicit `HStack(spacing: 12)`:
      - Icon: `Image(systemName:)`, `.symbolRenderingMode(.hierarchical)`, `.frame(width: 18, height: 18)`
      - Label: size 13, weight `.medium` (selected `.semibold` via existing `isSelected`)
    - Row padding: horizontal 10, vertical 8; fixed row height ~36 (use `.frame(height: 36)` on row content)
    - Keep `.hubSidebarNavRow(isSelected:)` — selected label color comes from modifier (accent per D-03)

    Unselected label must be `.primary` (modifier already uses primary when not selected).
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/AppShell/ToolSidebarView.swift | rg -c 'symbolRenderingMode\(\.hierarchical\)|frame\(width: 18, height: 18\)|\.quaternary' | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>Rows are icon+label with spec sizing; section label more subtle.</done>
</task>

<task type="auto">
  <name>Task 3: HelperToolsHealthStrip tokens + CI</name>
  <files>Sources/NikoMusicHub/AppShell/HelperToolsHealthStrip.swift, Sources/NikoMusicHub/AppShell/ToolSidebarView.swift</files>
  <action>
    Per S-08 and spec §5.4:
    - Title "Helper Tools": size 10, weight `.semibold`, `.foregroundStyle(.tertiary)`
    - Replace 8px `Circle()` dots with 7px dots using `HubDesignSystem.Size.statusDot` (keep existing state colors on Circle fills)
    - Tool lines: size 10 for label and status text
  - Card padding in ToolSidebarView: inner `.padding(10)` (was 12); outer `.padding(.horizontal, 8)` and `.padding(.bottom, 10)` (was 10/14)

    Do not change health-check async logic or Homebrew copy unless required for layout.
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/AppShell/HelperToolsHealthStrip.swift | rg -c 'statusDot|\.tertiary' | awk '{exit ($1>=2)?0:1}' && ./script/ci.sh</automated>
  </verify>
  <done>Health strip matches §5.4; CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local bundle metadata | Version string from Info.plist only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | I | Bundle version display | accept | Read-only CFBundleShortVersionString |
</threat_model>

<verification>
- `./script/ci.sh`
</verification>

<success_criteria>
- SH-02 satisfied per ROADMAP success criteria #2
</success_criteria>

<output>
After completion, create `.planning/phases/20-shell-navigation/20-02-SUMMARY.md`
</output>
