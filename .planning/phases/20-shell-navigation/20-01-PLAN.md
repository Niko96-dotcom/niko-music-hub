---
phase: 20-shell-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AppCore/Components/HubToolLayout.swift
  - Tests/AppCoreTests/HubToolContentColumnTests.swift
  - Sources/NikoMusicHub/AppShell/AppShellView.swift
  - Sources/NikoMusicHub/NikoMusicHubApp.swift
autonomous: true
requirements:
  - SH-01
must_haves:
  truths:
    - "Three-column shell uses 10px gaps between glass panels"
    - "Active tool content is centered in a max-width column without editing each tool view"
    - "Collapsed sidebar rails show accentTint hover respecting reduce motion"
    - "Default window opens at 1280×820"
  artifacts:
    - path: Sources/AppCore/Components/HubToolLayout.swift
      provides: "HubToolContentColumn view modifier"
      contains: "hubToolContentColumn"
    - path: Sources/NikoMusicHub/AppShell/AppShellView.swift
      provides: "Shell chrome with centered tool column"
    - path: Sources/NikoMusicHub/NikoMusicHubApp.swift
      provides: "defaultSize 1280×820"
  key_links:
    - from: AppShellView.activeToolView
      to: hubToolContentColumn()
      via: "View extension on feature content"
      pattern: "hubToolContentColumn"
    - from: AppShellView.collapsedRail
      to: HubDesignSystem.Colors.accentTint
      via: "onHover background"
      pattern: "accentTint"
---

<objective>
Deliver shared tool-column centering and shell chrome updates for SH-01 without touching individual tool feature views.

Purpose: Wave 2 shell cohesion per UI-REDESIGN-PLAN §4 and decisions S-01–S-05.
Output: `HubToolContentColumn` modifier, updated `AppShellView`, window default size, regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-shell-navigation/20-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/UI-REDESIGN-PLAN.md
@.planning/phases/19-design-system-foundation/19-01-SUMMARY.md

<interfaces>
From `Sources/AppCore/Components/HubToolLayout.swift`:
```swift
public enum HubToolLayout {
    public static let maxContentWidth: CGFloat = 680
    public func hubToolContentPadding() -> some View
}
```

From `Sources/NikoMusicHub/AppShell/AppShellView.swift`:
- `HStack(spacing: HubDesignSystem.Spacing.shell)` — shell gap already 10 per S-01; verify, do not regress to 14.
- `activeToolView` uses `.frame(maxWidth: .infinity, alignment: .topLeading)` — replace centering strategy per S-02.
- `collapsedRail` is plain `Button` + `hubGlassPanel` — add hover per S-03.
- `shellToolbar` sidebar toggle `Image(systemName:)` — add `.symbolRenderingMode(.hierarchical)` per S-04.

From `Sources/NikoMusicHub/NikoMusicHubApp.swift`:
- `.defaultSize(width: 1_420, height: 860)` → `1_280` × `820` per S-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubToolContentColumn contract + tests</name>
  <files>Sources/AppCore/Components/HubToolLayout.swift, Tests/AppCoreTests/HubToolContentColumnTests.swift</files>
  <behavior>
    - New public `View` extension `hubToolContentColumn()` exists on HubToolLayout.swift
    - Source contains `HubToolLayout.maxContentWidth` and pattern centering inner column (double `.frame(maxWidth: .infinity)` per spec §4.2)
    - Tests compile and pass after implementation
  </behavior>
  <action>
    Add `hubToolContentColumn()` to AppCore (same file as `HubToolLayout` per discretion S-02 placement).

    Implementation per spec §4.2 / S-02:
    ```swift
    // Outer: fill available width; inner VStack/HStack content:
    .frame(maxWidth: HubToolLayout.maxContentWidth, alignment: .topLeading)
    .frame(maxWidth: .infinity) // centers constrained column
    ```
    Apply `hubToolContentPadding()` on the outer container so tools keep consistent insets without per-tool edits.

    Do NOT modify BPM/converter/recorder/downloader/settings/archive views in this plan.
  </action>
  <verify>
    <automated>swift test --filter HubToolContentColumnTests</automated>
  </verify>
  <done>Modifier exported from AppCore; tests assert symbol exists in HubToolLayout.swift source.</done>
</task>

<task type="auto">
  <name>Task 2: AppShellView centering, rails, toolbar symbols</name>
  <files>Sources/NikoMusicHub/AppShell/AppShellView.swift</files>
  <action>
    Per S-01: Confirm `HubDesignSystem.Spacing.shell` is 10 on `HStack` and outer `.padding` — fix only if drifted.

    Per S-02: Wrap `feature.makeView(context:)` (and empty-state fallback) with `.hubToolContentColumn()`. Change center column frame from `.topLeading` fill-only to allow horizontal centering of the constrained column (keep `minWidth: 420`, `layoutPriority(1)`).

    Per S-03: In `collapsedRail`, add `@State private var isHovered` (or local hover state), `.onHover`, background `HubDesignSystem.Colors.accentTint` when hovering. Animate with `withAnimation` only when `!accessibilityReduceMotion` (carry D-08 from Phase 19).

    Per S-04: Toolbar sidebar toggle labels: `.symbolRenderingMode(.hierarchical)` on both `Image(systemName:)` buttons.

    Keep `minWindowWidth` logic unchanged per S-05.
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/AppShell/AppShellView.swift | rg -c 'hubToolContentColumn|accentTint|symbolRenderingMode' | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>Shell applies shared centering wrapper; collapsed rails and toolbar match §4.3–4.4.</done>
</task>

<task type="auto">
  <name>Task 3: Default window size + CI</name>
  <files>Sources/NikoMusicHub/NikoMusicHubApp.swift</files>
  <action>
    Per S-05: Change `.defaultSize(width: 1_280, height: 820)` in `NikoMusicHubApp` (was 1420×860).

    Run full gate after shell changes.
  </action>
  <verify>
    <automated>rg -v '^[[:space:]]*//' Sources/NikoMusicHub/NikoMusicHubApp.swift | rg -c 'defaultSize\(width: 1_280, height: 820\)' | awk '{exit ($1>=1)?0:1}' && ./script/ci.sh</automated>
  </verify>
  <done>App entry declares 1280×820; `./script/ci.sh` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local UI | Shell layout only; no new network or file writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | I | AppShellView | accept | No user-supplied strings in shell chrome changes |
</threat_model>

<verification>
- `swift test --filter HubToolContentColumnTests`
- `./script/ci.sh`
</verification>

<success_criteria>
- SH-01: 10px shell gap, centered tool column via wrapper, rail hover, hierarchical toolbar icons, 1280×820 default
</success_criteria>

<output>
After completion, create `.planning/phases/20-shell-navigation/20-01-SUMMARY.md`
</output>
