---
phase: 26-downloader-command-truth
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
files_modified:
  - Sources/FeatureDownloader/YtDlpDownloader.swift
  - Sources/FeatureDownloader/YtDlpDownloadCommandBuilder.swift
  - Sources/FeatureDownloader/DownloaderUseCase.swift
  - Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift
  - Tests/FeatureDownloaderTests/DownloaderUseCaseTests.swift
  - Tests/FeatureDownloaderTests/YtDlpOutputCollectorTests.swift
autonomous: true
requirements:
  - CMD-04
  - CMD-05
must_haves:
  truths:
    - "Simulate/preflight uses the same format selector and extra args as real download"
    - "Both simulate and download pass --no-playlist"
    - "Simulate keeps 30s timeout; non-zero exit surfaces stderr via ytDlpFailureMessage"
    - "Split UTF-8 chunks across consume() calls still produce correct lines and file markers"
    - "finish() reparses accumulated stdout/stderr when streaming missed final NIKO_MUSIC_HUB_FILE marker"
  artifacts:
    - path: Sources/FeatureDownloader/YtDlpDownloadCommandBuilder.swift
      provides: "Shared format-aware args for simulate and download"
    - path: Sources/FeatureDownloader/DownloaderUseCase.swift
      provides: "Format-aware simulate with --no-playlist"
    - path: Sources/FeatureDownloader/YtDlpDownloader.swift
      provides: "UTF-8-safe YtDlpOutputCollector with full-text reparse on finish"
    - path: Tests/FeatureDownloaderTests/YtDlpOutputCollectorTests.swift
      provides: "Split UTF-8 and finish-time reparse coverage"
  key_links:
    - from: DownloaderUseCase.simulateAndEnqueue
      to: YtDlpDownloadCommandBuilder.simulateArguments
      via: "builds --simulate request from options.formatSelection"
      pattern: "--no-playlist"
    - from: YtDlpOutputCollector.finish
      to: accumulated stdout/stderr buffer
      via: "reparse full text for NIKO_MUSIC_HUB_FILE markers"
      pattern: "NIKO_MUSIC_HUB_FILE:"
---

<objective>
Make preflight format-aware with playlist guardrails and harden output collection across split UTF-8 chunks.

Purpose: Simulate must validate the selected format path and block playlist expansion (CMD-04). Output collector must not lose final file markers when UTF-8 splits across pipe chunks (CMD-05).
Output: Shared command builder, updated simulate gate, UTF-8-safe collector with finish() fallback, full CMD requirement test matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-downloader-command-truth/26-CONTEXT.md
@Sources/FeatureDownloader/DownloaderUseCase.swift
@Sources/FeatureDownloader/YtDlpDownloader.swift
@Sources/FeatureDownloader/DownloadFormatSelection.swift

<interfaces>
From DownloadFormatSelection.swift:
```swift
public enum YtDlpFormatArgumentBuilder {
    public static func arguments(for selection: DownloadFormatSelection) -> YtDlpFormatArguments
}
```

Current simulate (DownloaderUseCase ~line 88):
```swift
arguments: ["--simulate", "--print", "%(title)s", url.absoluteString]
// Missing: format args, --no-playlist
```

Locked simulate policy:
- Same format selector + extra args as download
- Add `--no-playlist` to simulate and download
- Keep `timeoutSeconds: 30` on simulate
- Non-zero exit → `ytDlpFailureMessage`

Locked output collection:
- UTF-8 pending-line buffer (keep)
- Accumulate full stdout/stderr; reparse on `finish()` as fallback
- Primary path: `NIKO_MUSIC_HUB_FILE:` print markers
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YtDlpDownloadCommandBuilder + format-aware simulate tests</name>
  <files>Sources/FeatureDownloader/YtDlpDownloadCommandBuilder.swift, Sources/FeatureDownloader/DownloaderUseCase.swift, Tests/FeatureDownloaderTests/DownloaderUseCaseTests.swift</files>
  <behavior>
    - Test: simulate args for `.audioOnly/.wav` include `-f`, wav post-process extras, `--no-playlist`, `--simulate`
    - Test: simulate args for `.videoWithAudio` include video format selector and `--no-playlist`
    - Test: simulate request keeps `timeoutSeconds: 30`
    - Test: simulate failure still does not enqueue; stderr surfaced in error
    - Test: download args from builder also include `--no-playlist`
  </behavior>
  <action>
    Create `YtDlpDownloadCommandBuilder` (per discretion — centralize shared args):

    - `static func downloadArguments(for request: DownloadRequest) -> [String]` — extract current arg-building from `YtDlpDownloader.download` including `--no-playlist` after format args.
    - `static func simulateArguments(formatSelection: DownloadFormatSelection, sourceURL: URL, ffmpegLocationURL: URL?) -> [String]` — `--simulate`, `--no-playlist`, same `-f` + extra args as download, `--print`, `%(title)s`, URL. Omit output path / progress / file print markers.

    Update `DownloaderUseCase.simulateAndEnqueue` to build simulate args via builder using `options.formatSelection` and resolved ffmpeg location (per locked simulate decision). Keep 30s timeout.

  Refactor `YtDlpDownloader.download` to call `YtDlpDownloadCommandBuilder.downloadArguments` instead of inline arg array (add progress/file-print flags in downloader or builder — keep NIKO_PROGRESS template from 26-01).

    Add `DownloaderUseCaseTests.testSimulateUsesFormatSelectionAndNoPlaylist` with `CapturingSimulateRunner` recording arguments for wav vs video selections.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter DownloaderUseCaseTests 2>&1 | tail -10</automated>
  </verify>
  <done>CMD-04: Simulate validates selected format path and includes --no-playlist on simulate + download.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UTF-8-safe collector with finish() reparse</name>
  <files>Sources/FeatureDownloader/YtDlpDownloader.swift, Tests/FeatureDownloaderTests/YtDlpOutputCollectorTests.swift</files>
  <behavior>
    - Test: UTF-8 multi-byte character split across two `consume()` calls reassembles to one line
    - Test: `NIKO_MUSIC_HUB_FILE:` marker split across chunks is found after second consume
    - Test: marker only in accumulated buffer (never emitted as complete line during stream) is recovered in `finish()`
    - Test: emoji or non-ASCII in path (fixture string) resolves correctly
  </behavior>
  <action>
    Enhance `YtDlpOutputCollector` (private class in YtDlpDownloader.swift — expose test seam via `internal` test helper or package-visible factory if needed):

    1. Keep `pending` string buffer for incomplete lines across chunks.
    2. Add `accumulatedOutput: String` — append every raw chunk in `consume()` (and track separately from line draining).
    3. On `finish()`:
       - Process remaining `pending` as today.
       - Run `reparseAccumulatedOutput()`: split `accumulatedOutput` on newlines, run `outputPathCandidates` on each line (and final partial), merge any new paths into `candidatePaths` without duplicates.
       - Then resolve file URLs on disk as today.

    For UTF-8 safety: when appending chunks, use `String` only after validating complete UTF-8 sequences OR accumulate as `Data` and decode with `.utf8` allowing partial sequences at end of `pending`. Prefer Data-based pending buffer if simpler — must handle split emoji/CJK across chunk boundary.

    Create `YtDlpOutputCollectorTests` using a package-visible `YtDlpOutputCollectorTesting` shim (e.g. `internal struct YtDlpOutputCollectorHarness` in FeatureDownloader with `@testable import`) to call consume/finish without running yt-dlp.

    Do not change primary streaming path behavior for well-formed single-chunk lines.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter YtDlpOutputCollectorTests 2>&1 | tail -8</automated>
  </verify>
  <done>CMD-05: Split UTF-8 and finish-time reparse recover final file markers.</done>
</task>

<task type="auto">
  <name>Task 3: CMD requirement verification matrix + CI</name>
  <files>Tests/FeatureDownloaderTests/DownloadProgressParsingTests.swift, Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift, Tests/FeatureDownloaderTests/DownloadStallMonitorTests.swift, Tests/FeatureDownloaderTests/DownloaderUseCaseTests.swift, Tests/FeatureDownloaderTests/YtDlpOutputCollectorTests.swift</files>
  <action>
    Audit all CMD-01..CMD-05 requirements map to automated tests:

    | Req | Evidence test(s) |
    |-----|------------------|
    | CMD-01 | DownloadProgressParsingTests NIKO_PROGRESS cases; YtDlpDownloaderTests template args; DownloaderUseCase progress test |
    | CMD-02 | YtDlpDownloaderTests nil timeout |
    | CMD-03 | DownloadStallMonitorTests; YtDlpDownloaderTests stall fakes |
    | CMD-04 | DownloaderUseCaseTests simulate format + --no-playlist |
    | CMD-05 | YtDlpOutputCollectorTests split UTF-8 + finish reparse |

    Fill any gaps found during audit with focused tests (do not duplicate). Update `StreamingDestinationRunner` in YtDlpDownloaderTests to use `NIKO_MUSIC_HUB_FILE:` marker instead of only `[download] Destination:` where appropriate.

    Run full FeatureDownloader test target and `./script/ci.sh`.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && ./script/ci.sh 2>&1 | tail -20</automated>
  </verify>
  <done>All five CMD requirements have deterministic automated evidence; CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yt-dlp file path output → URL resolution | Paths from external tool; resolved under output directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | I | outputPathCandidates | mitigate | Resolve paths relative to outputDirectory; reject empty paths |
| T-26-06 | T | --no-playlist | mitigate | Prevents accidental multi-file playlist download expansion |
</threat_model>

<verification>
- `swift test --filter FeatureDownloaderTests` green
- `./script/ci.sh` green
- CMD-01..CMD-05 each mapped to at least one automated test
</verification>

<success_criteria>
- CMD-04: Simulate uses format args + `--no-playlist`; download also includes `--no-playlist`.
- CMD-05: Collector handles split UTF-8; `finish()` reparse recovers `NIKO_MUSIC_HUB_FILE:` markers.
- Phase 26 success criteria 4–5 satisfied; full CMD matrix documented in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/26-downloader-command-truth/26-03-SUMMARY.md`
</output>
