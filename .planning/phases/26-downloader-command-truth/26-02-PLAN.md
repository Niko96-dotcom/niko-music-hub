---
phase: 26-downloader-command-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - Sources/FeatureDownloader/YtDlpDownloader.swift
  - Sources/FeatureDownloader/DownloadStallMonitor.swift
  - Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift
  - Tests/FeatureDownloaderTests/DownloadStallMonitorTests.swift
autonomous: true
requirements:
  - CMD-03
must_haves:
  truths:
    - "Silent downloads with no output or progress for 120 seconds fail with a clear stall message"
    - "Any collector activity or NIKO_PROGRESS line resets the stall clock"
    - "Stall detection is testable without real-time sleeps in CI"
  artifacts:
    - path: Sources/FeatureDownloader/DownloadStallMonitor.swift
      provides: "Injectable clock stall window (120s) with activity reset"
    - path: Sources/FeatureDownloader/YtDlpDownloader.swift
      provides: "Download integrates stall monitor alongside streaming runner"
    - path: Tests/FeatureDownloaderTests/DownloadStallMonitorTests.swift
      provides: "Deterministic stall tests using fake clock"
  key_links:
    - from: YtDlpOutputCollector.consume
      to: DownloadStallMonitor.recordActivity
      via: "every chunk and parsed line resets stall timer"
      pattern: "recordActivity"
    - from: DownloadStallMonitor
      to: download cancellation
      via: "throws DownloadError.downloadFailed with stall message"
      pattern: "Download stalled — no progress for 2 minutes"
---

<objective>
Add stall-aware protection for silent/stuck downloads without reintroducing a fixed total-duration kill.

Purpose: Replace the removed 90s stopwatch with activity-based failure when yt-dlp goes silent (CMD-03). Locked stall window: 120 seconds; user message: `Download stalled — no progress for 2 minutes`.
Output: `DownloadStallMonitor` with injectable clock, integrated into `YtDlpDownloader`, deterministic unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-downloader-command-truth/26-CONTEXT.md
@Sources/FeatureDownloader/YtDlpDownloader.swift
@Sources/AppCore/Services/ExternalProcessRunning.swift

<interfaces>
From 26-01 (must be complete):
- Download uses `timeoutSeconds: nil`
- Progress lines include `NIKO_PROGRESS:` prefix
- `YtDlpOutputCollector.consume` streams chunks to `progressHandler`

Stall policy (locked):
- Window: 120 seconds without progress marker OR collector activity
- Message: `Download stalled — no progress for 2 minutes`
- Injectable clock for tests (no `Task.sleep` in stall unit tests)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DownloadStallMonitor with fake clock tests</name>
  <files>Sources/FeatureDownloader/DownloadStallMonitor.swift, Tests/FeatureDownloaderTests/DownloadStallMonitorTests.swift</files>
  <behavior>
    - Test: no activity for 120s (fake clock advance) → `isStalled == true`
    - Test: activity at 119s resets window → not stalled at 120s from last activity
    - Test: `stallErrorMessage` equals `Download stalled — no progress for 2 minutes`
    - Test: advancing clock in 30s steps without sleep completes in &lt;1s wall time
  </behavior>
  <action>
    Create `DownloadStallClock` protocol: `var now: Date { get }` (or monotonic `TimeInterval`).

    Create `DownloadStallMonitor`:
    - `static let stallWindowSeconds: TimeInterval = 120`
    - `init(clock: DownloadStallClock)`
    - `func recordActivity()` — reset last-activity timestamp
    - `func checkStalled() -> Bool` — true when `now - lastActivity >= 120`
    - `var stallErrorMessage: String` — exact locked copy

    Provide `FakeDownloadStallClock` in test target (mutable `currentTime`).

    Write tests first (RED), then minimal monitor implementation (GREEN). No `Task.sleep` in these tests — only manual clock advancement.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter DownloadStallMonitorTests 2>&1 | tail -8</automated>
  </verify>
  <done>Stall monitor logic verified deterministically with injectable clock.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate stall monitor into YtDlpDownloader</name>
  <files>Sources/FeatureDownloader/YtDlpDownloader.swift, Tests/FeatureDownloaderTests/YtDlpDownloaderTests.swift</files>
  <action>
    Extend `YtDlpDownloader` init with optional `stallClock: DownloadStallClock? = nil` (default `SystemDownloadStallClock()`). Keep public API stable — clock param has default.

    During `download`:
    1. Create `DownloadStallMonitor` from clock.
    2. Call `recordActivity()` on download start.
    3. Wrap `progressHandler` passed to collector: on each line, `recordActivity()`; if line parses as progress (`parseProgressPercentage` non-nil), also `recordActivity()` (same call — one activity hook is enough per line/chunk).
    4. In `consume` callback path, call `recordActivity()` on every chunk (even non-line chunks).

    Run a concurrent stall poll loop while streaming download executes:
    - Use `async let` or `Task` polling every ~5s (discretion) calling `checkStalled()`
    - On stall: cancel download task / terminate process via existing cancellation path; throw `DownloadError.downloadFailed(monitor.stallErrorMessage)`

    For non-streaming runner fallback, still run stall monitor against collector callbacks.

    Add `StallAfterSilenceRunner` fake in `YtDlpDownloaderTests`: streaming runner emits nothing; inject `FakeDownloadStallClock` and advance time past 120s from test code to trigger stall without sleeping. Assert error message contains `Download stalled — no progress for 2 minutes`.

    Add `ResetsStallOnProgressRunner`: emits one `NIKO_PROGRESS: 5.0%` line, advances clock &lt;120s, then completes — must not stall.
  </action>
  <verify>
    <automated>cd /Users/niko/Documents/Niko-Music-Hub && swift test --filter 'DownloadStallMonitorTests|YtDlpDownloaderTests' 2>&1 | tail -10</automated>
  </verify>
  <done>CMD-03: Stalled silent downloads fail with locked user-facing message; progress/output resets stall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stall monitor → download cancellation | Internal policy; false positive kills in-progress download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | D | DownloadStallMonitor | mitigate | Reset on any stdout/stderr chunk and parsed progress; 120s window per locked decision |
| T-26-04 | E | Stall error message | accept | Fixed user string; no external input in message |
</threat_model>

<verification>
- `swift test --filter DownloadStallMonitorTests` green without wall-clock sleeps
- `swift test --filter YtDlpDownloaderTests` green including stall integration fakes
</verification>

<success_criteria>
- CMD-03: Deterministic tests prove stall after 120s silence and recovery on activity; user sees `Download stalled — no progress for 2 minutes`.
</success_criteria>

<output>
After completion, create `.planning/phases/26-downloader-command-truth/26-02-SUMMARY.md`
</output>
