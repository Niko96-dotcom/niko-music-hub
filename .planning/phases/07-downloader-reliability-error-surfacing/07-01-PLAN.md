---
phase: "07"
plan: "01"
wave: 1
requirements: ["DL-08", "DL-09"]
---

# Plan 07-01: Simulate gate & stderr surfacing

## Tasks

1. Add `ytDlpFailureMessage(from:)` helper preferring non-empty stderr, trimmed.
2. On simulate non-zero exit, throw `downloadFailed` with stderr detail (not generic `unsupportedURL` only).
3. Add `DownloaderUseCaseTests`: simulate failure does not call `enqueue`; success enqueues once.

## Verification

- Unit tests pass for simulate gate and message formatting.
