---
phase: "16"
plan: "01"
status: done
---

# Plan 16-01: Sort, filters, health, CPR list, BPM

- `ArchiveBrowseSortMode`, `ArchiveBrowseFilter`, `ArchiveHealthReport`.
- Sidebar sort picker + filter chips; health panel.
- `SongDetailView` CPR list + BPM line; `MixdownBPMEstimator`.
