---
phase: 19-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/AppCore/Components/HubDesignSystem.swift
  - Tests/AppCoreTests/HubDesignSystemTokenTests.swift
autonomous: true
requirements:
  - DS-01
must_haves:
  truths:
    - "HubDesignSystem exposes spec §3.1 radius, spacing, size, color, and typography tokens"
    - "Warm indigo accent replaces ad-hoc accentColor on hub-owned selection tokens"
    - "Token values are locked by automated tests so regressions fail CI"
  artifacts:
    - path: Sources/AppCore/Components/HubDesignSystem.swift
      provides: "Full token surface per UI-REDESIGN-PLAN §3.1"
      contains: "enum Colors"
    - path: Tests/AppCoreTests/HubDesignSystemTokenTests.swift
      provides: "Token smoke assertions"
  key_links:
    - from: HubDesignSystem.Colors.accent
      to: selectedRowFill / selectedRowStroke
      via: "accentTint and selectedStroke static vars"
      pattern: "Colors\\.accentTint"
---

<objective>
Replace `HubDesignSystem` with the full §3.1 token set (radius, spacing, sizes, semantic colors, typography) and add unit tests.

Purpose: Every later phase composes from one token source — DS-01 foundation.
Output: Rewritten `HubDesignSystem.swift` + `HubDesignSystemTokenTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-design-system-foundation/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/UI-REDESIGN-PLAN.md
@Sources/AppCore/Components/HubDesignSystem.swift

<interfaces>
Current file is minimal (Radius 16/14/12, Spacing shell 14, Typography screenTitle 22). Replace entirely per spec §3.1 block — do not preserve legacy numeric values.

Key additions beyond today:
- `Radius.button`, `Spacing.cardGap`, `Spacing.controlGap`, `Spacing.inlineGap`
- `Size` enum (sidebarIconFrame, buttonMinHeight, iconButtonSize 30, chipHeight, statusDot 7, sidebar/inbox width ranges)
- `Colors` (accent RGB 0.35/0.42/0.95, accentTint, accentDeep, success, warning, danger, separator, cardStroke, selectedStroke)
- `Typography` (display, screenTitle 18, sectionTitle 14, body, bodySmall, caption, micro, mono)
- `glassStroke` → `Colors.cardStroke`; `glassInnerHighlight` remains but consumers will adapt in plan 02 (D-07)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token smoke tests (RED)</name>
  <files>Tests/AppCoreTests/HubDesignSystemTokenTests.swift</files>
  <behavior>
    - Radius.shell == 14, Radius.panel == 12, Radius.button == 8
    - Spacing.shell == 10, Spacing.section == 24
    - Size.iconButtonSize == 30, Size.statusDot == 7
    - Size.maxContentWidth is NOT on HubDesignSystem (lives on HubToolLayout) — do not assert here
    - Typography: screenTitle uses size 18 (inspect via Font — or assert documented constant if you add package-private test hooks; prefer comparing `HubToolLayout` in plan 04)
    - Colors.accent components approximately match spec warm indigo (red 0.35, green 0.42, blue 0.95) using NSColor/Color resolution on macOS or static CGFloat test constants exported only if needed
  </behavior>
  <action>
    Create `HubDesignSystemTokenTests` in AppCoreTests. Tests must fail against current HubDesignSystem (old radius 16, screenTitle 22, missing Size enum). Use XCTest only — no snapshot dependency.
    Per D-04: lightweight automated coverage, not manual-only.
  </action>
  <verify>
    <automated>swift test --filter HubDesignSystemTokenTests 2>&1 | head -20</automated>
  </verify>
  <done>Test target compiles; tests fail until Task 2 lands token values.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite HubDesignSystem per §3.1</name>
  <files>Sources/AppCore/Components/HubDesignSystem.swift</files>
  <action>
    Replace file contents with spec §3.1 structure (intent-first per D-01; match visual outcome, Swift shape may differ slightly).

    Per D-02: Apply typography sizes immediately — no legacy alias fonts, no compatibility shims.

    Per D-03: `Colors.accent` warm indigo; `selectedRowFill` = `Colors.accentTint`; `selectedRowStroke` = `Colors.selectedStroke`. Remove `Color.accentColor` from hub-owned statics in this file.

    Per D-01: If spec block uses `glassInnerHighlight` at fixed 0.10 white, keep as baseline; plan 02 will make panel/card highlights adaptive (D-07).

    Ensure all nested types are `public` for feature modules. Do not modify any feature or shell views in this phase.
  </action>
  <verify>
    <automated>swift test --filter HubDesignSystemTokenTests</automated>
  </verify>
  <done>All token smoke tests pass; `./script/ci.sh` still green for full suite.</done>
</task>

<task type="auto">
  <name>Task 3: CI gate</name>
  <files></files>
  <action>Run full local gate after token rewrite. Fix any compile errors in AppCore only (call sites may reference removed symbols — update only AppCore files in this plan's scope; if a non-AppCore file fails due to removed API, note for plan 02/04 but prefer minimal forward-compatible exports only if strictly required for compile).</action>
  <verify>
    <automated>./script/ci.sh</automated>
  </verify>
  <done>`./script/ci.sh` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local UI tokens | No network; tokens are compile-time constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | I | HubDesignSystem | accept | No secrets; public color constants only |
</threat_model>

<verification>
- `swift test --filter HubDesignSystemTokenTests` passes
- `./script/ci.sh` green
- Grep: `Color.accentColor` absent from `HubDesignSystem.swift` body (hub-owned tokens use `Colors.accent`)
</verification>

<success_criteria>
- DS-01 satisfied: all §3.1 token groups present and tested
- D-02, D-03 implemented in HubDesignSystem
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-system-foundation/19-01-SUMMARY.md`
</output>
