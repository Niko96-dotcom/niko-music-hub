---
phase: 19-design-system-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "19-03"
files_modified:
  - Sources/AppCore/Components/HubIconButton.swift
  - Sources/AppCore/Components/StatusDot.swift
  - Sources/AppCore/Components/HubToolLayout.swift
  - Sources/AppCore/Components/HubCompactChipColors.swift
  - Sources/FeatureArchiveBrowser/HubCompactChipColors+Archive.swift
  - Tests/AppCoreTests/HubSharedControlsTests.swift
autonomous: true
requirements:
  - DS-05
  - DS-06
  - DS-07
  - DS-08
must_haves:
  truths:
    - "Icon-only toolbar buttons are 30×30 with hover accent tint"
    - "Status dots are 7px and use semantic hub colors"
    - "Tool layout max width is 680 with top padding 16"
    - "HubCompactChipColors.archive lives in AppCore; archive extension file deleted"
  artifacts:
    - path: Sources/AppCore/Components/HubIconButton.swift
      provides: "Updated icon button sizes and hover"
    - path: Sources/AppCore/Components/StatusDot.swift
      provides: "7px semantic status colors"
    - path: Sources/AppCore/Components/HubToolLayout.swift
      provides: "680 max content width"
    - path: Sources/AppCore/Components/HubCompactChipColors.swift
      provides: "Merged .archive preset"
  key_links:
    - from: FeatureArchiveBrowser HubIconButton+ArchiveBrowse
      to: HubCompactChipColors.archive
      via: chipColors parameter
      pattern: "chipColors: \\.archive"
---

<objective>
Update shared controls (icon button, status dot, tool layout, chip colors) and merge archive chip preset into AppCore per Wave 1 steps 5–8.

Purpose: Complete DS-05–DS-08 without touching feature layouts beyond compile-safe chip import.
Output: Four updated AppCore files, deleted archive extension, control tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-design-system-foundation/19-CONTEXT.md
@docs/UI-REDESIGN-PLAN.md
@Sources/FeatureArchiveBrowser/HubCompactChipColors+Archive.swift
@Sources/FeatureArchiveBrowser/HubIconButton+ArchiveBrowse.swift

<interfaces>
// HubIconButton+ArchiveBrowse.swift uses chipColors: .archive — must resolve after merge to AppCore static.

// Current archive extension (DELETE after merge):
static let archive = HubCompactChipColors(
    selectedFill: ArchiveDesignTokens.accent, ...
)

// Replace with HubDesignSystem.Colors.accent per §7.6 — do NOT reference ArchiveDesignTokens (Phase 20 deletes that file).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared control tests (RED)</name>
  <files>Tests/AppCoreTests/HubSharedControlsTests.swift</files>
  <behavior>
    - HubToolLayout.maxContentWidth == 680
    - HubToolLayout.topPadding == 16
    - StatusDot frame uses Size.statusDot (7) — test via hosted view geometry or document constant on StatusDot if needed
    - HubCompactChipColors.archive.selectedFill equals HubDesignSystem.Colors.accent (or equivalent color comparison)
  </behavior>
  <action>Create tests; fail against current 720 width / 8px dot / missing .archive on main type.</action>
  <verify>
    <automated>swift test --filter HubSharedControlsTests 2>&1 | head -20</automated>
  </verify>
  <done>Tests written and failing pre-implementation.</done>
</task>

<task type="auto">
  <name>Task 2: HubIconButton + StatusDot (DS-05, DS-06, D-08)</name>
  <files>Sources/AppCore/Components/HubIconButton.swift, Sources/AppCore/Components/StatusDot.swift</files>
  <action>
    **HubIconButton** per §13.2:
    - Toolbar + compact chip frames: 30×30 (use `HubDesignSystem.Size.iconButtonSize`)
    - Font: toolbar 14 semibold (was 13); compactChip 13 semibold (was 12)
    - Toolbar hover: `@State private var isHovered` + `onHover`; background `Colors.accentTint` when hovered (not selected). Use `Color.accentColor` only where matching existing prominent bordered style is unavoidable for system chrome — prefer `Colors.accent` tint on prominent selected per D-03
    - Per D-08: hover transition uses `animation` only when `!accessibilityReduceMotion`; else instant
    - Preserve all accessibility: `accessibilityLabel`, toggle traits, `.help`, `isEnabled`
    - Discretion: inline hover vs shared helper — pick one, keep readable

    **StatusDot** per §13.3:
    - Frame 7×7 (`Size.statusDot`)
    - Colors: queued `.secondary`, running `Colors.accent`, completed `Colors.success`, failed `Colors.danger`, canceled `Colors.warning`
  </action>
  <verify>
    <automated>swift test --filter HubSharedControlsTests</automated>
  </verify>
  <done>Icon button and status dot match spec sizes/colors; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: HubToolLayout + chip merge (DS-07, DS-08)</name>
  <files>Sources/AppCore/Components/HubToolLayout.swift, Sources/AppCore/Components/HubCompactChipColors.swift, Sources/FeatureArchiveBrowser/HubCompactChipColors+Archive.swift</files>
  <action>
    **HubToolLayout** per §13.5: `topPadding = 16` (was 20); `maxContentWidth = 680` (was 720); keep horizontal/bottom/section spacing unchanged.

    **HubCompactChipColors** per §7.6 / DS-08:
    - Add `public static let archive = HubCompactChipColors(...)` using `HubDesignSystem.Colors.accent` for selected fill/stroke, `.white` selected foreground, unselected colors matching current archive extension (primary opacity fills)
    - Update `default` preset to use `Colors.accent` instead of `.accentColor` where hub-owned (D-03)
    - **Delete** `Sources/FeatureArchiveBrowser/HubCompactChipColors+Archive.swift`
    - Verify `HubIconButton+ArchiveBrowse.swift` still compiles with `chipColors: .archive` (static on AppCore type)
    - Do not delete `ArchiveDesignTokens.swift` (Phase 20 SH-04)

    No other FeatureArchiveBrowser view edits unless required for compile after extension removal.
  </action>
  <verify>
    <automated>test ! -f Sources/FeatureArchiveBrowser/HubCompactChipColors+Archive.swift && swift test --filter HubSharedControlsTests && ./script/ci.sh</automated>
  </verify>
  <done>Archive chip merged; extension file gone; CI green; feature views unchanged except compile fix if needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature module → AppCore | Archive browse uses shared chip colors only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | I | HubCompactChipColors | accept | Display-only tints; no user data |
</threat_model>

<verification>
- `HubCompactChipColors+Archive.swift` absent
- `rg '\.archive' Sources/FeatureArchiveBrowser/HubIconButton+ArchiveBrowse.swift` still matches
- `./script/ci.sh` green
- No edits under `Sources/FeatureBPMTapper`, `FeatureAudioConverter`, etc.
</verification>

<success_criteria>
- DS-05 through DS-08 complete
- Wave 1 steps 5–8 from §14 done
- D-08 reduce-motion on icon hover
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-system-foundation/19-04-SUMMARY.md`
</output>
