---
phase: 19-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "19-02"
files_modified:
  - Sources/AppCore/Components/HubLabeledButton.swift
  - Sources/AppCore/Components/HubSectionDivider.swift
  - Tests/AppCoreTests/HubDesignComponentsTests.swift
autonomous: true
requirements:
  - DS-03
  - DS-04
must_haves:
  truths:
    - "HubLabeledButton compiles with primary, secondary, and ghost styles"
    - "HubSectionDivider provides a 1pt separator using hub token color"
    - "New components are covered by AppCoreTests (D-04)"
  artifacts:
    - path: Sources/AppCore/Components/HubLabeledButton.swift
      provides: "Labeled icon+text actions for later tool phases"
      exports: ["HubLabeledButton", "HubLabeledButtonStyle"]
    - path: Sources/AppCore/Components/HubSectionDivider.swift
      provides: "Section break divider"
    - path: Tests/AppCoreTests/HubDesignComponentsTests.swift
      provides: "Component smoke tests"
  key_links:
    - from: HubSectionDivider
      to: HubDesignSystem.Colors.separator
      via: Rectangle fill
      pattern: "Colors\\.separator"
    - from: HubLabeledButton
      to: HubDesignSystem
      via: accent tint on ghost hover / prominent tint
      pattern: "HubDesignSystem"
---

<objective>
Add `HubLabeledButton` and `HubSectionDivider` per §13.1 and §13.6, with tests. No feature view adoption yet.

Purpose: DS-03 and DS-04 — shared primitives ready for phases 21–22 and Wave 5 error card.
Output: Two new public components + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-design-system-foundation/19-CONTEXT.md
@docs/UI-REDESIGN-PLAN.md
@Sources/AppCore/Components/HubIconButton.swift

<interfaces>
HubIconButton accessibility pattern to mirror:
- `accessibilityLabel` via Label text
- `.help(...)` when provided
- `role`, `isEnabled`, `action` closure

Discretion (CONTEXT): API may add `help: String?`, `isLoading` only if needed for compile — prefer minimal API matching §13.1: icon, label, style, role, isEnabled, action.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Component smoke tests (RED)</name>
  <files>Tests/AppCoreTests/HubDesignComponentsTests.swift</files>
  <behavior>
    - `HubSectionDivider()` can be instantiated (type-check / host in NSHostingController without crash)
    - `HubLabeledButton` init with each `HubLabeledButtonStyle` case compiles
    - Optional: assert `HubSectionDivider` body uses separator color via small test helper or snapshot-free hosting smoke (`XCTAssertNoThrow` hosting 1x1 frame)
  </behavior>
  <action>
    Add tests file; tests fail until components exist. Per D-04.
  </action>
  <verify>
    <automated>swift test --filter HubDesignComponentsTests 2>&1 | head -20</automated>
  </verify>
  <done>Tests exist and fail before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HubLabeledButton (DS-03, D-03)</name>
  <files>Sources/AppCore/Components/HubLabeledButton.swift</files>
  <action>
    Create `HubLabeledButton` per §13.1 intent (D-01):

    - `HubLabeledButtonStyle`: `.primary`, `.secondary`, `.ghost`
    - Body: `Button` + `Label(label, systemImage: icon)` with `.font(.system(size: 12, weight: .medium))`, `.controlSize(.small)`, `.disabled(!isEnabled)`
    - Map styles: primary → `.borderedProminent`, secondary → `.bordered`, ghost → custom plain style with hover background using `Colors.accentTint` (respect D-03 accent, not `.accentColor`)
    - Apply `.tint(HubDesignSystem.Colors.accent)` on prominent/bordered as needed
    - `buttonMinHeight` / `Radius.button` from tokens where practical
    - Per D-08: if ghost hover uses animation, honor `@Environment(\.accessibilityReduceMotion)` — instant state change when reduce motion is on
    - Preserve accessibility: label text is visible; add optional `help` mirroring HubIconButton
    - Public API: `public init(icon:label:style:role:isEnabled:action:)` (+ optional help if useful)

    Do not wire into BPM/converter/downloader views (deferred per CONTEXT).
  </action>
  <verify>
    <automated>swift test --filter HubDesignComponentsTests && ./script/ci.sh</automated>
  </verify>
  <done>HubLabeledButton compiles with all three styles; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Implement HubSectionDivider (DS-04)</name>
  <files>Sources/AppCore/Components/HubSectionDivider.swift</files>
  <action>
    Create public struct per §13.6 (§14 step 4 after LabeledButton):
    - `Rectangle().fill(HubDesignSystem.Colors.separator).frame(height: 1).padding(.vertical, 4)`
    - Public `init() {}`
    Do not replace existing `Divider()` usages in feature views this phase.
  </action>
  <verify>
    <automated>swift test --filter HubDesignComponentsTests && ./script/ci.sh</automated>
  </verify>
  <done>All component tests pass; CI green; components `public` from AppCore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Button actions | Caller-supplied closures; no automatic network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | D | HubLabeledButton action | accept | Local UI; caller validates side effects |
</threat_model>

<verification>
- `swift test --filter HubDesignComponentsTests` passes
- `./script/ci.sh` green
- New files listed in Package.swift only if SPM requires explicit sources (SwiftPM auto-discovers Sources/AppCore)
</verification>

<success_criteria>
- DS-03 and DS-04 met
- D-04 tests present
- No feature view files modified
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-system-foundation/19-03-SUMMARY.md`
</output>
