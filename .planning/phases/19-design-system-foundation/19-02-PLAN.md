---
phase: 19-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - Sources/AppCore/Components/HubGlassChrome.swift
autonomous: true
requirements:
  - DS-02
must_haves:
  truths:
    - "Shell ambient gradients are deeper in dark mode and balanced in light mode"
    - "Glass panels have stronger lift; cards show subtle top highlight"
    - "Sidebar selected rows use hub accent tokens, not system accentColor"
  artifacts:
    - path: Sources/AppCore/Components/HubGlassChrome.swift
      provides: "HubShellBackground, HubGlassPanel, HubGlassCard, HubSidebarNavRow"
  key_links:
    - from: HubGlassPanel
      to: HubDesignSystem.glassStroke
      via: strokeBorder overlay
      pattern: "glassStroke"
    - from: HubSidebarNavRow
      to: HubDesignSystem.Colors.accentTint
      via: selected fill/stroke
      pattern: "Colors\\.accentTint"
---

<objective>
Tune `HubGlassChrome` gradients, shadows, card highlight, and sidebar row selection per §3.2 with explicit light/dark parity.

Purpose: DS-02 glass depth before shell phase composes panels.
Output: Updated `HubGlassChrome.swift` only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-design-system-foundation/19-CONTEXT.md
@docs/UI-REDESIGN-PLAN.md
@Sources/AppCore/Components/HubGlassChrome.swift
@Sources/AppCore/Components/HubDesignSystem.swift

Depends on plan 19-01 for `HubDesignSystem.Colors` and `glassStroke`/`selectedRow*` tokens.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell background gradients (D-05)</name>
  <files>Sources/AppCore/Components/HubGlassChrome.swift</files>
  <action>
    Update `HubShellBackground` ambient colors per §3.2:

    Dark primary: `Color(red: 0.12, green: 0.13, blue: 0.19).opacity(0.65)` (was ~0.55).
    Dark secondary: `Color(red: 0.08, green: 0.10, blue: 0.15).opacity(0.50)` (was ~0.45).

    Light mode: tune for parity (D-05) — keep readable, non-muddy gradients; do not leave light mode at old values while only dark changes.

    Per D-06: Do not change `.thickMaterial` / `.thinMaterial` tiers.
  </action>
  <verify>
    <automated>swift build --target AppCore 2>&1 | tail -5</automated>
  </verify>
  <done>Shell background uses spec dark opacities; light mode visually tuned.</done>
</task>

<task type="auto">
  <name>Task 2: Panel shadow + card highlight (D-06, D-07)</name>
  <files>Sources/AppCore/Components/HubGlassChrome.swift</files>
  <action>
    `HubGlassPanel` shadow per §3.2: radius 18 (was 14), y 6 (was 5), dark opacity 0.40 (was 0.35). Keep material tier `.thickMaterial`.

    `HubGlassCard`: after stroke overlay, add top `LinearGradient` highlight per §3.2 (`Color.white.opacity(0.06)` → clear). Per D-07: make panel top highlight **and** card highlight adaptive via `@Environment(\.colorScheme)` — stronger in dark, subtler or omitted in light (panel already has highlight; align both).

    `HubSidebarNavRow`: selected foreground uses `HubDesignSystem.Colors.accent` (D-03); fill/stroke use `selectedRowFill` / `selectedRowStroke` (already mapped to accent tokens in 19-01). Remove `Color.accentColor` from selected foreground.
  </action>
  <verify>
    <automated>test "$(rg -c 'radius: 18' Sources/AppCore/Components/HubGlassChrome.swift 2>/dev/null || echo 0)" -ge 1</automated>
  </verify>
  <done>Panel shadow constants updated; card has top highlight; sidebar row uses hub accent tokens.</done>
</task>

<task type="auto">
  <name>Task 3: CI gate</name>
  <files></files>
  <action>Run `./script/ci.sh`. Do not edit feature views, shell views, or `StandardErrorCard` this phase.</action>
  <verify>
    <automated>./script/ci.sh</automated>
  </verify>
  <done>Full CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local rendering | Glass modifiers are view-only; no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | D | HubGlassChrome | accept | UI availability only; no security impact |
</threat_model>

<verification>
- `./script/ci.sh` green
- `rg 'Color\.accentColor' Sources/AppCore/Components/HubGlassChrome.swift` returns no matches in `HubSidebarNavRow` selected styling
</verification>

<success_criteria>
- DS-02 satisfied per §3.2 shell, panel, card, sidebar row
- D-05, D-06, D-07 honored
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-system-foundation/19-02-SUMMARY.md`
</output>
