---
quick_id: 260523-2ec
description: fix Testing
status: complete
must_haves:
  truths:
    - swift test compiles and runs all package test targets
    - XCTest is used consistently (no broken Swift Testing migration)
  artifacts:
    - Package.swift
    - scripts/test.sh
    - Tests/**/*.swift
  key_links:
    - Package.swift test targets -> Tests/* directories
---

# Quick Plan: fix Testing

## Task 1: Restore XCTest and Package targets

**files:** Package.swift, Tests/** (revert Swift Testing), Tests/FeatureDownloaderTests/** (XCTest rewrite)

**action:** Revert migrated tests to XCTest; add missing test targets; wire FeatureDownloader + FeatureAudioConverter deps.

**verify:** `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test` compiles

**done:** Build succeeds without `no such module 'Testing'` or `XCTest` errors

## Task 2: Fix compile and flaky integration tests

**files:** FeatureAudioRecorderTests/*, FeatureDownloaderTests/YtDlpHealthCheckerTests.swift, Sources/FeatureDownloader/YtDlpDownloader.swift

**action:** Add imports, `@unchecked Sendable` mocks, `@MainActor` VM tests, XCTSkip for permission-gated recording, fix health-check expectations.

**verify:** `swift test` — 0 failures

**done:** 149 tests executed, 0 failures (skipped OK)

## Task 3: Document test invocation

**files:** scripts/test.sh

**action:** Set DEVELOPER_DIR to Xcode.app when using Command Line Tools default.

**verify:** `./scripts/test.sh` passes

**done:** Script exits 0
