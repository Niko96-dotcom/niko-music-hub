#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "E2E user smoke is not implemented yet. Composer executor must replace this with a real fixture app-launch smoke before v0.1 is done." >&2
exit 2
